# Story 1.31: 업로드 데이터 기반 지식베이스 자동 구성 (FR33-B)

## Status

Review (2026-08-05, 구현+단위테스트+정적검증 완료, 실서버 IV만 잔여)

## 관련 문서

- [SELF_SERVICE_RAG_INTELLIDECISION_ADVANCEMENT_RESEARCH.md](../design/SELF_SERVICE_RAG_INTELLIDECISION_ADVANCEMENT_RESEARCH.md)
- [self-service-ai-assistant-prd.md](../product/self-service-ai-assistant-prd.md) FR33-B, NFR8
- [1.26.knowledge-base-document-crud-and-upload.story.md](1.26.knowledge-base-document-crud-and-upload.story.md) — 선행 Story(업로드 원본 데이터)
- [1.28.knowledge-graph-n-hop-generalization.story.md](1.28.knowledge-graph-n-hop-generalization.story.md) — 선행 Story(`document`/`api_endpoint` 노드 타입)
- 코드(신규 예정): `src/ai_voicebot/self_service/knowledge_base_assembler.py`(가칭)

## Story

**As a** 신규 테넌트(다른 시스템을 운영 중일 수도 있는) 관리자,
**I want** API/Tool 정보·매뉴얼·화면 설명이 담긴 상세 데이터를 업로드하기만 하면,
**so that** 이 시스템(SIP PBX 셀프서비스)에 종속되지 않은 채로 (1) 이용 매뉴얼 Q&A, (2) AI가 변경
가능한 설정 목록, (3) 화면 안내 3종의 지식베이스가 자동 구성되어 AI가 곧바로 응대할 수 있다.

## Acceptance Criteria

1. 업로드된 문서(Story 1.26의 `knowledge_documents`, 특히 `OpenApiSpecAdapter`로 파싱된 API/Tool
   스펙과 일반 매뉴얼 텍스트)로부터 다음 3종을 자동 추출·생성하는 파이프라인이 구현된다:
   - (a) 매뉴얼 Q&A: 업로드된 자유 텍스트에서 질문-답변 쌍을 추출(기존 `manual_indexer.py` 태그
     파싱 패턴을 일반화하거나, LLM 기반 Q&A 추출 — 구체 방식은 Task 1 스파이크에서 결정).
   - (b) AI 변경 가능한 설정 목록: OpenAPI 스펙의 쓰기 가능 엔드포인트(PUT/PATCH/POST 등)를
     "설정 항목" 형태(라벨/허용값/writable 여부)로 매핑. 실제 실행부(get_fn/update_fn 콜러블)와의
     결합은 이번 범위에서 하지 않는다(Non-Goal, 아래 참고).
   - (c) 화면 안내: 업로드 데이터에 화면/메뉴 설명이 포함되어 있으면 `knowledge_graph.py`의
     `document`/`api_endpoint` 노드로 등록되어 Screen Graph 스타일 안내에 활용 가능해야 한다.
2. 이 자동 구성은 **도메인 비종속**이어야 한다 — SIP PBX 전용 키워드/도메인 이름을 하드코딩하지
   않고, 업로드된 데이터의 구조(태그·섹션 제목·OpenAPI 필드명)만으로 동작해야 한다.
3. 자동 구성 결과는 Story 1.23(지식베이스 현황 탭)에서 조회 가능해야 한다(문서 수·Q&A 수·설정
   항목 수·화면 노드 수).
4. **명시적 Non-Goal(참고사항 반영)**: 업로드된 "AI 변경 가능한 설정 목록"이 실제 시스템의 진짜
   쓰기 동작과 정확히 매칭되어 실행되는 것은 이번 Story의 범위가 아니다 — 지식베이스 구성과
   Q&A/화면 안내 응대까지만 보장하며, 실제 Tool 실행 매칭은 후속 과제로 명시적으로 이연한다.
5. 기존 SIP PBX 테넌트(owner 9001/9003 등)의 하드코딩 카탈로그/Screen Graph 기반 응대는 회귀
   없이 그대로 유지되어야 한다(자동 구성은 새 데이터 소스에 대한 **추가** 경로이지 기존 경로의
   대체가 아니다).

## Tasks / Subtasks

- [x] Task 1: Q&A 자동 추출 방식 확인 — Story 1.25/1.26이 이미 `MarkdownManualAdapter`/`OpenApiSpecAdapter`로 도메인 비종속 Q&A 추출을 구현해둘(스파이크 불필요, 코드 직접 확인으로 결론)
- [x] Task 2: OpenAPI 스펙 → "설정 항목" 매핑 로직 구현(`knowledge_base_assembler.py::classify_setting_item`, HTTP 메서드 기반 writable 추정)
- [x] Task 3: 화면 안내 노드 확인 — Story 1.28의 `document` 노드/`relates_to` 엣지(catalog_domain→document, domain_tags 매칭)가 이미 도메인 비종속으로 동작함을 코드 재확인(`api_endpoint` 노드는 미사용 예약 상태 유지, 별도 Story로 이연 가능)
- [x] Task 4: 지식베이스 현황 탭에 자동 구성 통계 노출(`GET /knowledge-base/inventory`의 `auto_assembled` 필드 추가, 기존 응답 구조 무수정) + 프론트엔드 카드/표 추가
- [x] Task 5: 임의 도메인(이커머스 주문 API 샘플) End-to-End 단위테스트로 도메인 비종속성 실증(`TestDomainAgnosticEndToEnd`)
- [ ] Task 6: 기존 테넌트 회귀 확인 + 실서버 IV

## Dev Notes

- "참고사항: 업로드되는 데이터 기반 동작해야 하고 실제 동작사항은 달라도 됨 — 해당 매칭은 추후
  고려 사항이며 도메인 독립사항이 우선" — 사용자가 명시적으로 우선순위를 구분했으므로, Tool
  실제 실행 매칭에 시간을 쓰지 말고 지식베이스 구성 자동화에 집중할 것.
- 도메인 비종속성 검증 방법: SIP PBX와 무관한 가상의 API 스펙(예: 이커머스 주문 관리 시스템)을
  샘플로 넣어 지식베이스가 구성되는지 스파이크로 직접 실행 확인할 것(코드에 SIP PBX 키워드가
  섞여 들어가면 이 검증에서 드러난다).

## QA Results

**2026-08-05, Copilot(BMAD Dev 역할) 구현 검증**:
- 신규 `src/ai_voicebot/self_service/knowledge_base_assembler.py`: `classify_setting_item()`(질문 문구 정규식 기반, `OpenApiSpecAdapter`가 생성하는 "METHOD path 엔드포인트는 무엇을 하나요?" 포맷과 정확히 일치) + `summarize_auto_assembled_knowledge_base()`(Story 1.23 `summarize_inventory` 패턴과 동일하게 "이미 조회된 raw 항목 리스트"를 받는 순수 함수, ChromaDB를 직접 열지 않음).
- **핵심 설계 결정**: 업로드 매체(`add_knowledge`)가 `answer`/`section_title`를 메타데이터로 저장하지 않음을 코드 직접 확인하고(`knowledge_service.py::add_knowledge`), 대신 `text` 필드의 "Q: ...\nA: ..." 원문을 분해해 질문/답변을 복원하도록 구현(추측 금지, 실제 저장 형식 직접 확인 후 구현).
- **AC1-c 범위 축소 결정(명시적 기록)**: Story 1.28이 이미 `document` 노드 타입+`relates_to` 엣지(catalog_domain→document, domain_tags 문자열 매칭만으로 동작하는 도메인 비종속 구조)를 구현해둘을 코드 재확인 — AC1-c("document/api_endpoint 노드로 등록")를 이미 충족함으로 판단. `api_endpoint` 노드/`documents` 엣지는 Story 1.28에서 이미 예약만 되고 리솔버는 빈 상태로 남아있어("예약, 후속 Story") 이번 Story에서 추가 구현하지 않음 — 엔드포인트 단위 세밀 노드화는 실수요 확인 후 별도 Story로 이연.
- API: `GET /api/settings/ai-assistant/knowledge-base/inventory` 응답에 `auto_assembled`(선택적 필드, 기존 필드 무수정) 추가. 동일 엔드포인트가 이미 가져온 `raw_all`/`document_records`를 재사용해 추가 ChromaDB/DB 라운드트립 없음.
- 프론트엔드: "지식베이스 현황" 탭에 자동 구성 통계 카드 4개(매뉴얼 Q&A/설정 후보/쓰기가능 후보/화면노드수) + 설정 항목 표 추가(기존 컴포넌트 패턴 재사용, 신규 의존성 없음).
- **검증**: 신규 단위테스트 10건(`test_knowledge_base_assembler.py`) 모두 통과 — 특히 `TestDomainAgnosticEndToEnd`는 SIP PBX와 무관한 가상의 이커머스 주문 API 스펙으로 AC2(도메인 비종속)를 실증. `tests_new/unit/test_ai_voicebot -k "self_service or knowledge"` 54건 전체 통과(회귀 없음). `npx tsc --noEmit`/`npx eslint` 0에러.
- **잔여**: 실서버 IV(사용자 승인 후 재시작, 실제 업로드한 OpenAPI 문서로 `auto_assembled` 필드 실측 확인) 미실시.
