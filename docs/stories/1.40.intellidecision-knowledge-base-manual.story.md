# Story 1.40: IntelliDecision 설명 매뉴얼 전환 (FR34-D)

## Status

Draft

## 변경 이력

- 2026-08-05: 신규 생성 — 사용자 추가 요청 반영. 기존 "AI 의사결정 로직" 탭(Story 1.32)의
  시뮬레이션 트리거 방식을 폐기하고, 실제 LLM 호출 없이 지식베이스 기반 정적 사례로 설명하는
  매뉴얼로 전환한다(FR34-D).

## 관련 문서

- [self-service-ai-assistant-prd.md](../product/self-service-ai-assistant-prd.md) FR34-D
- [1.18.intellidecision-policy-transparency-tab.story.md](1.18.intellidecision-policy-transparency-tab.story.md), [1.32.intellidecision-simulator-ux-overhaul.story.md](1.32.intellidecision-simulator-ux-overhaul.story.md) — 부분 대체 대상(유형 A~I 하위 탭 구조는 재사용, 시뮬레이션 트리거는 제거)
- [1.31] Story 1.31 KB 자동 구성(질문 예시/사례 데이터 출처)
- [1.28.knowledge-graph-n-hop-generalization.story.md](1.28.knowledge-graph-n-hop-generalization.story.md) — 사례 hop 경로 산출에 재사용

## Story

**As a** AI 도우미가 각 유형(A~I)을 어떻게 판단하고 응대하는지 이해하고 싶은 테넌트 관리자,
**I want** "AI 의사결정 로직" 탭에서 실제 질문을 입력해 시뮬레이션을 돌리지 않고도, 유형별 정의와
실제 업로드된 지식베이스 기반 사례 설명을 바로 확인하기를,
**so that** LLM 호출 비용/대기시간 없이 즉시, 그리고 실제 데이터에 기반한 신뢰할 수 있는 설명을
얻을 수 있다.

## Acceptance Criteria

1. "AI 의사결정 로직" 탭에서 기존 Story 1.32의 "질문을 입력해 시뮬레이션 실행" UX를 제거한다
   (실시간 LLM 호출 없음).
2. 유형 A~I 각각에 대해: 정의/트리거 조건(`GET /intellidecision-policy`의 `trigger_examples` 등
   기존 정책 메타데이터 재사용, 신규 API 불필요)과 함께, **실제 업로드된 지식베이스에서 추출한
   사례 설명**을 정적으로 표시한다 — 예: "이런 질문이 들어오면 → 이 지식베이스 문서가 매칭되고
   → 이런 hop이 확장되어 → 이렇게 응답한다"를 실제 KB 데이터 기반으로 사전 계산해 보여준다.
3. 사례 설명에 사용되는 hop 경로는 Story 1.28 `traverse_graph()`를 사전(또는 조회 시점에) 호출해
   산출하되, 이는 어디까지나 "지식베이스 구조 설명"이지 실시간 대화 시뮬레이션이 아니다 — 응답
   생성(LLM 호출)은 발생하지 않는다.
4. KB에 특정 유형에 대응하는 문서/사례가 없으면 "아직 사례가 없습니다"로 명확히 표시하고, 임의로
   지어내지 않는다.
5. 실제 응대를 확인하고 싶은 사용자는 Story 1.39의 실제 채팅 패널로 안내하는 링크/버튼을 제공한다
   (매뉴얼과 실제 채팅의 역할 분리를 명확히 함).

## Tasks / Subtasks

- [ ] Task 1: 기존 Story 1.32 시뮬레이션 트리거 UI 제거, 유형 A~I 하위 탭 구조는 유지
- [ ] Task 2: 유형별 KB 기반 사례 설명 데이터 산출 로직 설계(정적 사전 계산 vs 조회 시점 계산 결정)
- [ ] Task 3: 사례 설명 UI(정의+트리거 예시+KB 사례+hop 경로) 구현
- [ ] Task 4: 사례 없음 상태 처리, 실제 채팅 패널로의 유도 링크 추가
- [ ] Task 5: 정적 검증 + 실서버 IV

## Dev Notes

- 이 Story는 Story 1.32를 완전히 대체하는 것이 아니라, "시뮬레이션 트리거" 부분만 제거하고 유형
  A~I 하위 탭 구조·정책 메타데이터 재사용 패턴은 그대로 유지한다.
- LLM 호출이 전혀 없어야 한다는 점이 이 Story의 핵심 제약이다 — 사례 설명은 KB/그래프 데이터
  조회만으로 구성한다.

## QA Results

(미착수)
