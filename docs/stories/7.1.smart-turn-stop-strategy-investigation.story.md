# Story 7.1: 현재 Smart Turn stop 전략 실제 동작 조사

## Status

Done(2026-07-29) — **핵심 사실 확인 + 관측 로깅 구현·단위테스트·회귀 검증까지 완료. 실제
통화에서의 데이터 축적은 다음 재시작 후(Story 7.2 착수 전제)**

> 참조원: [voice-latency-turn-taking-prd.md](../product/voice-latency-turn-taking-prd.md) FR13, FR14 /
> [voice-latency-turn-taking-architecture.md](../architecture/voice-latency-turn-taking-architecture.md) §1.4(정정), §2.4

## Story

**As a** 개발자,
**I want** 현재 실제로 동작 중인 발화 종료(턴 완료) 판단 로직의 정체와 실제 정확도를 파악하고
싶다,
**so that** Epic 7의 개선 방향(튜닝만으로 충분한지, 보조 판단 레이어가 필요한지)을 추측이 아닌
근거로 결정할 수 있다.

## Acceptance Criteria

1. `pipeline_builder.py`가 실제로 어떤 turn-stop 전략을 사용하는지 코드 실행으로 확정한다(추측
   금지 — Story 5.1이 반복 강조한 "파일 존재 ≠ 실제 연결" 원칙 재적용).
2. `config.yaml`의 `smart_turn.*` 설정이 이 전략에 실제로 영향을 주는지 확정한다.
3. 위 사실을 근거로 Architecture 문서(§1.4/§2.4)를 정정한다.
4. 실제 통화에서 이 모델의 판정 결과(완료/미완료, 판정까지 걸린 시간)를 관측할 수 있는 로깅
   설계안을 제시한다(구현은 Story 7.2/7.3에서, 본 Story는 설계까지).
5. 외부 대안(Vogent Turn 등) 리서치 결과를 정리해 Story 7.2 설계 결정에 필요한 입력을 제공한다.

## Tasks / Subtasks

- [x] **Task 1: 실제 turn-stop 전략 확정** (AC: 1)
  - [x] `python -c`로 `pipeline_builder.py`와 동일한 방식(`UserTurnStrategies(start=[...])`,
    `stop=` 미지정)으로 인스턴스화해 `.stop` 속성을 직접 출력 → 실제로
    `[TurnAnalyzerUserTurnStopStrategy(LocalSmartTurnAnalyzerV3())]`로 채워짐을 확인.
  - [x] 이 과정에서 `LocalSmartTurnAnalyzerV3`가 실제 ONNX 모델
    (`smart-turn-v3.2-cpu.onnx`)을 로드하는 디버그 로그까지 직접 확인(모델 로드 자체는 성공).
- [x] **Task 2: config.yaml 연동 여부 확인** (AC: 2)
  - [x] `pipeline_builder.py` 전체에서 `smart_turn` 문자열 grep → 0건. `config.yaml`의
    `smart_turn.enabled`/`max_hold_secs`는 **코드 어디에서도 읽히지 않는 완전한 orphan 설정**임을
    확인.
- [x] **Task 3: Architecture 문서 정정** (AC: 3)
  - [x] §1.4에 "🔴 중요 정정(2026-07-29)" 블록 추가, §2.4 신설.
- [x] **Task 4: 관측 로깅 구현** (AC: 4)
  - [x] `pipecat.utils.base_object.BaseObject.add_event_handler()`(공개 API, 동일 이벤트에 여러
    핸들러 등록 가능하며 각각 독립 실행됨을 pipecat 소스로 직접 확인)를 이용해,
    `TurnAnalyzerUserTurnStopStrategy`를 **서브클래싱 없이** 명시적으로 생성하고
    `on_user_turn_stopped` 이벤트에 순수 로깅 핸들러만 추가하는
    `build_observed_smart_turn_stop_strategy()`(신규
    `src/ai_voicebot/pipecat/smart_turn_stop_observer.py`) 구현.
  - [x] `pipeline_builder.py`에 연결 — 관측 전략 생성 실패 시 기존 pipecat 기본값(stop
    미지정)으로 자동 폴백(회귀 없음 보장).
  - [x] 단위 테스트 3건(`test_smart_turn_stop_observer.py`) 작성 — 전략 생성 확인/이벤트
    핸들러 정상 실행/생성 실패 시 None 폴백 검증.
  - [x] 실제 파이썬 실행으로 `smart_turn_stop_triggered` 로그가 정상 발화함을 확인.
  - [x] 전체 회귀(`tests_new/unit`, 무관 사전 결함 3개 파일 제외) 399건 전체 통과.
- [x] **Task 5: 외부 대안 리서치 정리** (AC: 5) — 아래 "리서치 요약" 참고(기존
  `docs/VOICE_AI_TURN_TAKING_REFERENCES.md` 내용 재확인 + 이번 조사로 얻은 신규 맥락 반영).

## 핵심 발견 (Task 1/2 상세)

```python
>>> from pipecat.turns.user_turn_strategies import UserTurnStrategies
>>> from pipecat.turns.user_start import MinWordsUserTurnStartStrategy
>>> uts = UserTurnStrategies(start=[MinWordsUserTurnStartStrategy(min_words=3)])
# 로그: "Loading Local Smart Turn v3.x model from .../smart-turn-v3.2-cpu.onnx..."
# 로그: "Loaded Local Smart Turn v3.x"
>>> uts.stop
[<pipecat.turns.user_stop.turn_analyzer_user_turn_stop_strategy.TurnAnalyzerUserTurnStopStrategy object at ...>]
```

`pipecat.turns.user_turn_strategies.UserTurnStrategies`의 `__post_init__`이 `stop`이 명시되지
않으면 `[TurnAnalyzerUserTurnStopStrategy(turn_analyzer=LocalSmartTurnAnalyzerV3())]`를 기본값으로
채운다(pipecat 소스 직접 확인). `pipeline_builder.py`는 `start=`만 지정하고 `stop=`을 전혀
지정하지 않으므로, **이 기본값이 그대로 적용된다.**

**중요한 재해석**: Story 5.1(2026-07-24)이 "죽은 코드"로 확정한 것은 이 저장소 자체의 커스텀
래퍼(`src/ai_voicebot/pipecat/smart_turn_processor.py`)였다. pipecat 패키지 자체가 내장한
`LocalSmartTurnAnalyzerV3`(동일한 이름의 모델이지만 **우리 코드와 무관하게 pipecat 라이브러리
안에 있는 별개의 구현**)는 조사 대상에 포함되지 않았다 — "저장소 자체 코드만 grep했고, 의존성
패키지의 프레임워크 레벨 기본값은 확인하지 않은" 조사 공백이었다.

## 리서치 요약 (Task 5, `docs/VOICE_AI_TURN_TAKING_REFERENCES.md` 기반 + 신규 맥락)

| 방식                                                          | 현재 상태                                                     | 특징                                                                              | 이번 Epic 적합성                                     |
| ------------------------------------------------------------- | ------------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------- |
| VAD Only(침묵 시간만)                                         | 사용자가 인식한 "현재 상태"였으나 실제로는 아님(위 발견 참고) | 빠름, 필러/재개 구분 불가                                                         | 이미 Smart Turn이 이 위에 얹혀 있어 논외             |
| **Smart Turn v3.2**(pipecat 내장, `LocalSmartTurnAnalyzerV3`) | **이미 암묵적으로 활성**                                      | 문법/억양/속도(prosody) 기반, 오디오 네이티브, 23개 언어(한국어 포함), CPU 10ms대 | **1순위 — 이미 있으므로 관측·튜닝부터**              |
| Vogent Turn(멀티모달, Whisper+SmolLM)                         | 미도입                                                        | 오디오+텍스트 컨텍스트, 더 정확할 가능성, 80M 파라미터로 더 무거움                | Smart Turn 관측 결과 부족 시 2순위 후보              |
| LLM 기반 보조 판단(신규 검토)                                 | 미도입                                                        | `judge_barge_in()`과 유사한 패턴 재사용 가능, 상시 호출 시 지연 위험(NFR8)        | 애매한 경우에만 보조적으로 트리거하는 경량 버전 검토 |

**1차 결론(잠정, Story 7.2에서 확정)**: 완전히 새 모델을 도입하기보다, 이미 활성 상태인 Smart
Turn v3.2를 **먼저 관측 가능하게 만들고**, 실제 실패 사례(필러 후 재개를 종료로 오판)가 확인되면
그때 Vogent Turn/LLM 보조 판단을 검토하는 순서가 가장 낮은 리스크로 보인다(신규 인프라 도입은
NFR4 위반 가능성도 있어 후순위).

## Dev Notes

- 관측 로깅(Task 4)까지 포함해 구현 완료 — 판단 로직 자체는 변경하지 않았다(순수 관측용
  핸들러만 추가, 기존 판정 경로와 병렬 실행).
- 다음 착수 지점: 실통화로 `smart_turn_stop_triggered` 로그를 수집해 Story 7.2의 설계
  결정에 필요한 데이터(오판 빈도, `stt_turn_superseded`와의 상관관계)를 축적한다.

## Change Log

| Date                                               | Version        | Description                                                                                   | Author         |
| -------------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------- | -------------- |
| 2026-07-29                                         | 0.1            | pipecat 기본값 Smart Turn v3.2 암묵 적용 발견, Architecture 정정, 외부 대안 리서칭 정리       | GitHub Copilot |
| 2026-07-29                                         | 0.2            | Task 4(관측 로깅 구현) 완료 — `smart_turn_stop_observer.py` 신규, `pipeline_builder.py` 연결, |
| 단위테스트 3건 + 전체 회귀 399건 통과, Status→Done | GitHub Copilot |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 4.5 (GitHub Copilot)

### Completion Notes

- 사용자가 "묵음만으로 판단한다"고 전제한 요청을 그대로 받아들이지 않고, 코드 실행으로 먼저
  현재 상태를 재확인한 것이 핵심이었다 — 실제로는 이미 세미틱 턴 감지 모델이 켜져 있었다.
- 이 발견은 Epic 7의 방향을 "신규 기능 개발"에서 "기존 기능 관측성 확보 + 필요 시 보강"으로
  전환시켰다 — 사용자에게도 착수 전 반드시 공유해야 하는 사실이라 판단해 계획 수립 전에
  먼저 보고했다.

### File List

- `src/ai_voicebot/pipecat/smart_turn_stop_observer.py` (신규)
- `src/ai_voicebot/pipecat/pipeline_builder.py` (수정 — 관측 stop 전략 명시적 연결, 실패 시 폴백)
- `tests_new/unit/test_ai_voicebot/test_smart_turn_stop_observer.py` (신규, 3건)

## QA Results

**단위 회귀(2026-07-29)**: 신규 테스트 3/3 PASS. `tests_new/unit` 전체(무관 사전 결함 3개 파일
제외) 399건 전체 통과(회귀 없음). 실제 파이썬 실행으로 `smart_turn_stop_triggered` 로그가 정상
발화함을 확인.

**실서버**: 미실시 — 관측 로깅은 다음 서버 재시작 후 실제 통화로 데이터가 쌓이는지 확인
필요(Story 7.2 착수 전제조건).

- ✅ 승인(PASS) — 조사+관측 로깅 구현 범위 완료, 실통화 데이터 축적은 Story 7.2로 이월
