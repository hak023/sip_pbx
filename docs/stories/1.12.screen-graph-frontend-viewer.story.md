# Story 1.12: Screen Graph 프론트엔드 열람

## Status

Done

## Story

**As a** 테넌트 관리자,
**I want** AI 도우미가 어떤 화면을 안내할 수 있는지 프론트엔드에서 직접 확인하기를,
**so that** AI가 설명하는 내용이 실제 화면과 일치하는지 신뢰할 수 있다.

## Acceptance Criteria

1. `sip-pbx/frontend/app/settings/ai-assistant/docs`(Story 1.9에서 만든 도움말 페이지)에 **신규 탭 "화면 안내"**가 추가되어 Screen Graph 데이터(도메인별 라우트·설명·UI 요소)를 읽기 전용으로 표시한다.
2. 화면 정보가 있는 도메인은 실제 라우트 링크(클릭 시 해당 설정 화면으로 이동)를 함께 제공한다.
3. 기존 설정 페이지 컴포넌트/레이아웃 컨벤션을 따른다(CR3).

### Integration Verification

- **IV1**: 신규 탭 추가가 기존 "이용 매뉴얼 Q&A"/"AI 변경 가능 설정" 탭의 동작에 영향을 주지 않는다.

## Tasks / Subtasks

- [x] **Task 1: 백엔드 API 확장** (AC: 1)
  [Source: `src/api/routers/settings_ai_assistant.py` — 어제 구현한 `/catalog` 엔드포인트와 병렬 구조]
  - [x] `GET /api/settings/ai-assistant/screen-graph` 신규 — `screen_graph.list_all_screens()`를
        JSON으로 반환(도메인, 라우트, 설명, UI 요소 목록)

- [x] **Task 2: 프론트엔드 탭 추가** (AC: 1, 2, 3)
  [Source: `frontend/app/settings/ai-assistant/docs/page.tsx` — 기존 탭 배열에 추가]
  - [x] `Tab` 타입에 `"screen"` 추가, 탭 버튼 목록에 "화면 안내" 추가
  - [x] 화면 안내 탭 렌더링: 도메인별 카드(라우트 링크 + 설명 + UI 요소 목록)
  - [x] 화면 정보가 없는 도메인(persona)은 "전용 설정 화면 없음(지식 베이스에서 관리)" 안내 표시

- [x] **Task 3: 회귀 확인** (AC: IV1)
  - [x] 기존 탭("이용 매뉴얼 Q&A", "AI 변경 가능 설정") 정상 동작 확인
  - [x] TypeScript 컴파일 오류 없음 확인

## Dev Notes

### Previous Story Insights

- Story 1.9/어제 작업에서 이미 `settings/ai-assistant/docs/page.tsx`에 탭 기반 구조(`qa` |
  `catalog`)가 구축되어 있다 — 본 Story는 세 번째 탭만 추가하면 되므로 페이지 자체를 새로 만들
  필요가 없다.
- `settings_ai_assistant.py`의 `/catalog` 엔드포인트가 `settings_catalog.py`를 그대로 노출하는
  패턴을 그대로 미러링해 `/screen-graph`가 `screen_graph.py`를 노출한다.

### File Locations

```
src/api/routers/settings_ai_assistant.py                 # 수정 — /screen-graph 엔드포인트 추가
frontend/app/settings/ai-assistant/docs/page.tsx          # 수정 — "화면 안내" 탭 추가
```
[Source: [architecture#component-architecture](../architecture/self-service-ai-assistant-architecture.md)]

### Technical Constraints

- **CR3**: 기존 `settings/*` 페이지의 Next.js App Router·폼/리스트 컴포넌트 컨벤션을 따른다.
- 본 Story는 Story 1.11(Screen Graph 백엔드 구축)에 의존한다 — 순서상 1.11이 선행되어야 한다.

## Change Log

| Change    | Date       | Version | Description                                                                      | Author                  |
| --------- | ---------- | ------- | -------------------------------------------------------------------------------- | ----------------------- |
| 초안 생성 | 2026-07-16 | 0.1     | 사용자 요청(전체 BMAD 프로세스 진행, 프론트엔드 열람 요건 추가) 기반 Story 작성  | Copilot (BMAD SM 역할)  |
| 구현 완료 | 2026-07-16 | 0.2     | `/screen-graph` API 및 프론트엔드 "화면 안내" 탭 구현, TypeScript 오류 없음 확인 | Copilot (BMAD Dev 역할) |

## Dev Agent Record

### Agent Model Used

GitHub Copilot (Claude Sonnet 4.5)

### Completion Notes

- 기존 `settings/ai-assistant/docs/page.tsx`의 탭 구조(`qa`/`catalog`)에 `screen` 탭을 자연스럽게
  추가했다 — 페이지 신규 생성 없이 기존 컴포넌트 재사용(CR3 준수).
- 화면 정보가 있는 도메인은 라우트 링크를 제공하고, 없는 도메인(persona 등)은 안내 문구로
  명시해 혼동을 방지했다.
- (2026-07-20) 실브라우저 검증 완료 — 별도 QA 문서 참조.

### File List

- `src/api/routers/settings_ai_assistant.py` (수정 — `/screen-graph` 엔드포인트)
- `frontend/app/settings/ai-assistant/docs/page.tsx` (수정 — "화면 안내" 탭)

## QA Results

**2026-07-20 실브라우저 통합 검증 완료.** 상세: [self-service-ai-assistant-screen-graph-qa-plan.md](../qa/self-service-ai-assistant-screen-graph-qa-plan.md) §4-3

- SG-FE-01: "화면 안내" 탭 클릭 시 6개 도메인 카드(라우트 링크·설명·UI 요소) 정상 렌더링 확인(스크린샷 검증)
- SG-FE-02: 라우트 링크 클릭 시 `/settings/ai-escalation` 등 실제 설정 화면으로 정상 이동 확인
- SG-FE-03: 기존 "이용 매뉴얼 Q&A"/"AI 변경 가능 설정" 탭 회귀 없음 확인(IV1 충족)
- 참고: 최초 페이지 로드 직후 첫 클릭이 반영되지 않는 것처럼 보인 현상을 1회 관찰했으나 새로고침
  후 재현되지 않아 결함으로 확정하지 않음(QA 문서에 참고 기록)

본 Story를 이것으로 **완료**로 판정한다.
