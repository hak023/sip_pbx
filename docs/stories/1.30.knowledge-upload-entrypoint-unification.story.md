# Story 1.30: 지식 업로드 진입점 통합 + 테넌트/도메인 공통 고정 콘텐츠 구분 (FR33-A/C)

## Status

Review (2026-08-05, 구현+정적검증 완료, 실서버 IV만 잔여)

## 관련 문서

- [SELF_SERVICE_RAG_INTELLIDECISION_ADVANCEMENT_RESEARCH.md](../design/SELF_SERVICE_RAG_INTELLIDECISION_ADVANCEMENT_RESEARCH.md)
- [self-service-ai-assistant-prd.md](../product/self-service-ai-assistant-prd.md) FR33-A, FR33-C, NFR8
- [1.26.knowledge-base-document-crud-and-upload.story.md](1.26.knowledge-base-document-crud-and-upload.story.md) — "지식 업로드" 탭 원본
- Epic 2 Story 2.4/2.5 — "설정 관리" 탭 원본(`catalog_config`/`screen_graph` export/import)
- 코드(수정 예정): `frontend/app/settings/ai-assistant/docs/page.tsx`(탭 구조 재편, 신규 컴포넌트 로직 없음 — 기존 두 탭의 컴포넌트를 하위 섹션으로 재배치), 백엔드 API는 무수정(FR33-A는 UX 계층 통합, 데이터 모델 통합 아님)

## Story

**As a** 셀프서비스 AI 도우미를 운영하는 테넌트 관리자,
**I want** "지식 업로드"와 "설정 관리"로 나뉘어 있던 업로드 화면을 하나의 진입점에서 이용하고,
**so that** "테넌트 지식(매뉴얼/PDF/OpenAPI)"과 "시스템 공통 설정/구성(카탈로그·화면 안내)"을 헷갈리지 않고 한 화면에서 구분해 다룰 수 있다.

## Acceptance Criteria

1. `settings/ai-assistant/docs` 최상위 탭에서 "지식 업로드"와 "설정 관리"가 하나의 "지식 업로드" 탭으로 통합된다(최상위 탭 개수 1개 감소).
2. 통합된 탭 내부에 소스 유형 선택 UI(예: 세그먼트 컨트롤/서브탭)가 있어 "① 테넌트 지식 문서"(Story 1.26 기존 업로드 폼·목록 그대로 재사용)와 "② 시스템 공통 설정/구성"(Epic 2 Story 2.4/2.5 export/import/버전 이력 그대로 재사용)을 전환해 볼 수 있다.
3. "② 시스템 공통 설정/구성" 섹션에는 FR33-C에 따라 "시스템 표준(테넌트 무관 고정)" 배지가 표시되고, 어떤 항목이 테넌트별로 커스터마이즈 가능한지/불가능한지 명확히 구분 표시된다(예: `intellidecision_policy` 유형 정의는 표준·읽기 전용, `catalog_config`/`screen_graph`는 테넌트가 편집 가능).
4. 기존 API 엔드포인트(`/api/knowledge-base/documents`, `/catalog-config/*`)는 변경하지 않는다(순수 프론트엔드 UX 재편, CR 원칙 — 회귀 위험 최소화).
5. 기존 두 탭 각각의 단위 동작(업로드/조회/수정/삭제, export/import/activate/rollback)은 재배치 후에도 100% 동일하게 동작해야 한다(Integration Verification).

## Tasks / Subtasks

- [x] Task 1: 탭 구조 리팩터링 설계 — 기존 두 탭의 JSX/상태를 그대로 유지한 채 컨테이너 컴포넌트만 재구성(AC1, AC2)
- [x] Task 2: 시스템 표준 배지 UI 추가 — `intellidecision_policy` 등 읽기 전용 항목과 편집 가능 항목 시각적 구분(AC3)
- [x] Task 3: `npx tsc --noEmit` / `npx eslint` 정적 검증
- [x] Task 4: 재배치 후 기존 두 탭 각 기능 코드 검토(AC5, 상태/핸들러/API 호출 전부 무수정 확인)
- [ ] Task 5: 실서버 IV — 사용자 승인 후 재시작, 브라우저로 통합 화면 확인

## Dev Notes

- 데이터 모델·API를 통합하지 말 것(NFR8) — 이번 Story는 UX 진입점 통합만이다. `knowledge_documents`와
  `self_service_catalog_config`는 목적(테넌트별 지식 vs 시스템 공통 구성 스키마)이 근본적으로 다르다.
- 프론트엔드 Playwright 자동 클릭은 이 저장소에서 WebSocket(8001) 재연결 루프로 상시 불안정
  (2026-07-20/28 기록 반복 확인) — `tsc`/`eslint` 정적 검증 + 수동 스크린샷으로 대체할 것.

## QA Results

**2026-08-05, Copilot(BMAD Dev 역할) 구현 검증**:
- `frontend/app/settings/ai-assistant/docs/page.tsx`: 최상위 `Tab` 타입에서 `"manage"` 제거(8개→7개
  탭), 신규 `UploadSection`(`"tenant"|"system"`) 내부 세그먼트 토글 추가. 기존 "지식 업로드"
  (Story 1.26)/"설정 관리"(Epic 2 Story 2.4/2.5) 콘텐츠는 한 줄도 삭제/변경 없이 그대로 이전
  (상태 변수, API 호출, 핸들러 전부 무수정).
- FR33-C: 시스템 공통 구성 섹션에 "시스템 표준(읽기 전용) vs 테넌트별 편집 가능" 안내 배너 추가
  (IntelliDecision 정책은 읽기 전용, 카탈로그/화면안내는 편집 가능임을 명시).
- 정적 검증: `npx tsc --noEmit` 0 에러, `npx eslint` 초기 2건(큰따옴표 미이스케이프) 발견→
  `&quot;`로 수정 후 0 에러/0 경고.
- **편집 중 발견한 사고(즉시 복구)**: `replace_string_in_file`로 중복 텍스트("설정 다운로드"
  블록)를 병합하려다 oldString이 모호하게 매칭되어 `{tab === "simulate" && (...)}` 전체 블록이
  버튼 JSX 자식 사이에 잘못 삽입되는 사고 발생 — `get_errors`/`tsc`가 즉시 잡지 못해(JSX 자체는
  문법적으로 유효) 수동 grep/read로 구조를 재확인해 발견 후 복구함. 교훈: 기존 블록과 거의 동일한
  중복 텍스트가 있는 파일에서 `replace_string_in_file`로 큰 블록을 재배치할 때는 oldString이
  유일하게 매칭되는지 반드시 확인하고, 편집 직후 반드시 `tsc --noEmit`으로 JSX 구조 교란 여부를
  즉시 재검증할 것.
- **잔여**: 실서버 IV(사용자 승인 후 재시작, 브라우저 확인) 미실시.

