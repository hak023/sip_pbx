# Story 1.9: 자동설정 변경 이력 프론트엔드 페이지

## Status

Done(2026-07-28 프론트엔드 "AI 도우미 변경 이력" 페이지 실브라우저 시각 확인 완료 — 아래 QA Results 참고)

## Story

**As a** 테넌트 관리자,
**I want** AI가 최근에 무엇을 바꿨는지 화면에서 확인하기를,
**so that** 대화로 변경한 내용을 신뢰하고 검증할 수 있다.

## Acceptance Criteria

1. `sip-pbx/frontend/app/settings/ai-assistant`(가칭) 신규 페이지에서 최근 자동설정 변경 이력이 목록으로 표시된다.
2. 각 항목은 변경 도메인·필드, 이전값→새값, 변경 시각, 관련 call_id를 표시한다.
3. 기존 설정 페이지 컴포넌트/레이아웃 컨벤션을 따른다(CR3).

### Integration Verification

- **IV1**: 신규 페이지 추가가 기존 `settings/*` 라우트의 네비게이션·레이아웃에 영향을 주지 않는다.

## Tasks / Subtasks

- [x] **Task 1: 조회용 REST 엔드포인트 추가** (AC: 1) — ✅ 완료 (2026-07-15)
  [Source: [architecture#tech-stack](../architecture/self-service-ai-assistant-architecture.md) — "Story 1.9만 프론트 조회용 API 1개 필요 시 기존 라우터 패턴 재사용"]
  - [x] `src/api/routers/self_service.py` 신규 — `GET /api/self-service/config-changes?owner=...&limit=...` 추가(`main.py`에 등록)
  - [x] `self_service_config_changes` 테이블(Story 1.8에서 생성)에서 `list_config_changes(owner, limit)`(기존 Story 1.8 함수 그대로 재사용, 새 조회 로직 없음)로 `owner` 필터 + `changed_at DESC` 정렬 조회

- [x] **Task 2: 프론트엔드 페이지 구현** (AC: 1, 2, 3) — ✅ 완료 (2026-07-15)
  [Source: `sip-pbx/frontend/app/settings/chat-relay/page.tsx` — 참고 컴벤션(`apiJson`/`getTenantOwner` 패턴, 로딩/에러 배너)]
  - [x] `sip-pbx/frontend/app/settings/ai-assistant/page.tsx` 신규 생성
  - [x] `chat-relay`/`persona` 등 기존 설정 페이지의 `useState`/`useEffect` + `apiJson` 기반 목록 로드 패턴 재사용
  - [x] 테이블 형태로 도메인·필드·이전값→새값·시각·call_id 표시(도메인명은 한글 라벨로 매핑)

- [x] **Task 3: 네비게이션 통합** (AC: IV1) — ✅ 완료 (2026-07-15)
  - [x] `frontend/components/AppHeader.tsx::SETTINGS_NAV`에 "셀프서비스 AI 도우미" 섹션 + 링크 추가(기존 항목 수정 없이 끝에 append)
  - [x] `SETTINGS_LINK_HREFS`/`settingsAreaActive`가 `SETTINGS_NAV`에서 자동 파생되므로 기존 라우트 목록·순서에 변경 없음(코드 구조상 IV1 보장)

## Dev Notes

### Previous Story Insights

Story 1.8에서 `self_service_config_changes` 테이블과 이중 로깅이 이미 구현되어 있다. 본 Story는 그 데이터를 **읽기만** 하면 된다 — 이번 Epic에서 유일하게 신규 REST 엔드포인트가 필요한 Story(다른 Story는 모두 기존 서비스 레이어 직접 호출로 충분).

### File Locations

```
src/api/routers/self_service.py (신규, 또는 기존 라우터에 엔드포인트 추가)
sip-pbx/frontend/app/settings/ai-assistant/page.tsx   # 신규
```
[Source: [architecture#source-tree](../architecture/self-service-ai-assistant-architecture.md)]

### Technical Constraints

- **CR3**: 기존 `settings/persona` 등의 Next.js App Router 컨벤션(`'use client'`, `useState`/`useEffect` 데이터 로드 패턴)을 그대로 따른다.
- 이 Story만 예외적으로 신규 REST 엔드포인트가 필요하다 — Architecture 문서의 "신규 엔드포인트 없음" 원칙에 대한 유일한 예외이며 사전에 문서화되어 있다.

## Testing

### Testing Standards

- **Test file location**: `tests_new/test_self_service_config_changes_api.py`(백엔드), 프론트엔드는 수동 검증(기존 프로젝트에 프론트 자동 테스트 관행 부재 — Dev가 발견 시 기존 관행 따름)
- **Framework**: pytest(백엔드)
- **필수 테스트 케이스**:
  1. `owner` 필터가 정확히 적용되어 다른 테넌트 이력이 섞이지 않음
  2. 정렬(`changed_at DESC`)이 올바름
  3. 페이지 렌더링 시 기존 `settings/*` 네비게이션에 회귀 없음(수동 확인)

## Change Log

| Date       | Version | Description                                                                                                                                                                                                              | Author                  |
| ---------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------- |
| 2026-07-14 | 0.1     | 최초 Draft 생성(시퀀싱 정정 후 1.8→1.9로 재배치)                                                                                                                                                                         | Copilot (BMAD SM 역할)  |
| 2026-07-15 | 0.2     | Task 1~3 구현 완료 — `GET /api/self-service/config-changes` 신규 엔드포인트(Epic 유일한 신규 REST), `settings/ai-assistant` 프론트 페이지, `AppHeader.tsx` 네비게이션 통합. 신규 백엔드 테스트 5건 통과. Status → Review | Copilot (BMAD Dev 역할) |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 4.5 (GitHub Copilot)

### Debug Log References

- `python -m pytest tests_new/unit/test_ai_voicebot/test_self_service_config_changes_api.py -v --no-cov` → 5 passed
- `python -m pytest tests_new/unit/test_ai_voicebot tests_new/unit/test_events -q --no-cov` → 169 passed, 회귀 없음

### Completion Notes List

- Task 1: `list_config_changes(owner, limit)`(Story 1.8에서 이미 구현됨)를 그대로 호출하는 얇은 라우터로 구현(새 조회 로직 없음, CR4/재사용 원칙 준수).
- Task 1: 실제 SQLite 파일 기반 통합 테스트로 owner 격리·`changed_at DESC` 정렬을 검증(Story 1.8의 테스트 패턴 재사용).
- Task 2: `persona`/`integrations` 페이지가 실제로는 리다이렉트뿐이라 참고 컴포넌트를 `chat-relay/page.tsx`(실제 데이터 로드 페이지)로 변경 — Dev Notes의 `persona/page.tsx` 참고 예시가 실제로는 빈 리다이렉트임을 확인.
- Task 3: `AppHeader.tsx`의 `SETTINGS_NAV`가 배열이고 `SETTINGS_LINK_HREFS`/`settingsAreaActive`가 이를 기반으로 파생되는 구조라, 새 heading+link+divider를 배열 끝에 추가하는 것만으로 기존 항목 변경 없이 통합 가능했다(IV1 코드 구조상 보장).

### File List

- `src/api/routers/self_service.py` (신규)
- `src/api/main.py` (수정 — `self_service_router` import/등록)
- `frontend/app/settings/ai-assistant/page.tsx` (신규)
- `frontend/components/AppHeader.tsx` (수정 — `SETTINGS_NAV`에 항목 추가)
- `tests_new/unit/test_ai_voicebot/test_self_service_config_changes_api.py` (신규, 5 tests)

## QA Results

**검토일**: 2026-07-15 | **검토자**: Copilot (BMAD PO/QA 역할 겸임, Epic 레벨 통합 검토)

AC/IV 전체 코드·테스트 대조 완료(백엔드 자동 테스트 5건 통과). **프론트엔드 시각 확인
완료(2026-07-28)**: 브라우저로 `/settings/ai-assistant`(owner=9003) 접속 → "AI 도우미 변경
이력" 표가 도메인 한글 라벨/필드/이전값→새값/변경시각/통화ID 컬럼과 함께 정상 렌더링되고,
같은 세션에서 방금 실행한 Undo 변경(id=28/29)까지 실시간으로 반영됨을 확인.

- ✅ 승인(PASS, 백엔드+프론트엔드 모두 확인)
