# Story 1.4: 설정 카탈로그 구축 (읽기 전용 등록)

## Status

Review

> **시퀀싱 정정 안내**: 본 Story는 원래 PRD 초안에서 Story 1.7의 일부였으나, "온보딩 체크리스트(구 1.4)가 아직 없는 카탈로그에 의존"하는 순서 오류를 SM 리뷰에서 발견하여 **본 Story로 앞당겨졌다**(PRD v0.3, 2026-07-14). 카탈로그의 **쓰기(자동설정 실행) 기능**은 Story 1.8에서 별도로 다룬다.

## Story

**As a** 테넌트 관리자,
**I want** (간접적으로) AI가 내 서비스의 모든 설정 항목이 무엇인지 이미 알고 있기를,
**so that** 이후 온보딩 안내·설정 조회·자동설정 기능이 특정 항목 몇 개에 국한되지 않는다.

## Acceptance Criteria

1. `settings_catalog.py`에 **7개 설정 도메인**(persona, ai-escalation, call-control, chat-relay, contacts, general, integrations) 각각의 **조회 함수**·필수/옵션 스키마·destructive 여부가 등록된다(변경 함수·자동설정 실행은 Story 1.8 범위).
2. `list_domains()`, `get_domain_schema(domain)`, `get_domain_value(domain, owner)` 3개 조회 API가 카탈로그를 통해 동작한다.
3. 카탈로그에 등록되지 않은 도메인 조회 시 명확한 오류(빈 스키마 또는 "미등록 도메인")를 반환한다.

### Integration Verification

- **IV1**: 카탈로그의 조회 함수는 각 도메인의 기존 서비스/라우터 함수를 오직 읽기(read-only)로만 호출하며 부작용이 없다.
- **IV2**: 7개 도메인 각각에 대해 최소 1개 필드의 조회 테스트가 통과한다(카탈로그 커버리지 검증).

## ⚠️ 도메인별 백엔드 함수 확정 — 이 Story의 핵심 조사 작업

Architecture 문서는 7개 도메인 중 **3개만 확인**했고 나머지 4개는 후보만 나열했다. 이 Story의 첫 작업은 나머지 4개를 실제로 검증하는 것이다.

| 도메인          | 상태           | 근거/후보                                                                                                                                                                                                                           |
| --------------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `chat-relay`    | ✅ 확인됨       | `src/services/chat_relay_service.py::get_chat_relay_settings(owner)`                                                                                                                                                                |
| `persona`       | ✅ 확인됨(추정) | `persona_service`(정확한 모듈 경로는 `src/api/routers/persona.py` import 확인 필요)                                                                                                                                                 |
| `call-control`  | 🔶 부분 확인    | `src/api/routers/call_control_api.py` — `list_rules(owner)`, `list_schedules(owner)`, `list_announcements(owner)` 등 라우터 레벨 함수 존재(서비스 모듈 분리 안 됨 — 라우터 함수를 직접 재사용하거나 데이터 접근 계층을 추출해야 함) |
| `ai-escalation` | ❌ 미확인       | 후보: `src/api/routers/hitl.py`, `src/api/routers/operator_status_api.py`                                                                                                                                                           |
| `contacts`      | ❌ 미확인       | 후보: `src/api/routers/caller_contacts.py`, `src/api/routers/contact_folders.py`                                                                                                                                                    |
| `general`       | ❌ 미확인       | 후보: `src/api/routers/tenants.py::get_tenant(tenant_id)`                                                                                                                                                                           |
| `integrations`  | ❌ 미확인       | 후보: `src/api/routers/google_calendar.py`(`src/services/gcal_service.py`)                                                                                                                                                          |

## ✅ 도메인별 백엔드 함수 확정 결과 (Task 0 조사 완료, 2026-07-15)

| 도메인          | 확정된 함수                                                                                                       | 비고                                                                                                                                                                      |
| --------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `persona`       | `src/ai_voicebot/knowledge/persona_service.py::PersonaService.get_persona(owner)` (async)                         | 별도 서비스 모듈 이미 존재                                                                                                                                                |
| `ai-escalation` | 동일 `get_persona(owner)`의 `escalation_mode`/`transfer_extension` 필드만 발췌                                    | 프론트엔드 `/settings/ai-escalation`도 `/api/persona/{owner}`를 그대로 사용함을 코드로 확인(persona.py 라우터 주석)                                                       |
| `call-control`  | `src/call_control/db.py::list_rules/list_schedules/list_announcements(owner)`                                     | 라우터 함수(`call_control_api.py`)가 이 3개를 그대로 호출하는 얇은 래핑임을 확인 → 데이터 계층을 직접 호출(FastAPI Query 우회)                                            |
| `chat-relay`    | `src/services/chat_relay_service.py::get_chat_relay_settings(owner)`                                              | Architecture에서 이미 확인됨                                                                                                                                              |
| `contacts`      | `src/common/caller_contact_db.py::list_caller_contacts` + `src/common/contact_folder_db.py::list_contact_folders` | 프론트 `/settings/contacts`는 `/contacts`로 리다이렉트되지만, 데이터 자체는 유효(라우터 모듈 내부 import를 직접 재사용)                                                   |
| `general`       | `src/api/routers/tenants.py::TENANTS_DATA` (정적 하드코딩 데이터, owner로 룩업)                                   | `get_tenant(tenant_id)` 라우터 함수 대신 원본 리스트를 직접 검색(async 오버헤드 불필요)                                                                                   |
| `integrations`  | `src/services/gcal_service.py::get_oauth_status(owner)`                                                           | 프론트 `/settings/integrations`는 `/settings/general`로 리다이렉트되지만, 카탈로그 관점에서는 "테넌트 프로필"(general)과 "외부 연동"(integrations)을 별개 도메인으로 유지 |

**중요 발견**: 프론트엔드 네비게이션은 `integrations`→`general`, `contacts`(settings)→`/contacts`로 통합되어 있어 UI 탭이 1:1로 매핑되지 않는다. 하지만 AC1이 명시한 7개 도메인은 **설정 개념 단위**(자동설정 시 destructive 판단이 도메인별로 다를 수 있으므로)로 그대로 유지하는 것이 타당하다고 판단해 7개 도메인을 모두 등록했다(구현 보고서 참고).

## Tasks / Subtasks

- [x] **Task 0: 미확인 4개 도메인 백엔드 함수 확정** (선행 조사, 모든 AC의 전제조건) — ✅ 완료 (2026-07-15, 위 표 참고)
  - [x] `hitl.py`/`operator_status_api.py`는 ai-escalation과 무관(HITL 큐/운영자 상태 API)함을 확인 — 실제 ai-escalation 설정(escalation_mode)은 persona 객체에 저장됨을 `ai-escalation/page.tsx` 주석에서 확인
  - [x] `caller_contacts.py`/`contact_folders.py`에서 contacts 도메인 조회 함수 확인(`list_caller_contacts`, `list_contact_folders`)
  - [x] `tenants.py`에서 general 도메인(테넌트 기본 정보) 조회 함수 확인(`TENANTS_DATA` 정적 리스트)
  - [x] `google_calendar.py`/`gcal_service.py`에서 integrations 도메인 조회 함수 확인(`get_oauth_status`)
  - [x] `call_control_api.py`는 데이터 접근 계층(`src/call_control/db.py`)을 직접 호출하는 방식으로 결정(라우터 대신 데이터 계층 직접 import — FastAPI Query 의존성 우회)

- [x] **Task 1: `settings_catalog.py` 레지스트리 골격 구현** (AC: 1, 2) — ✅ 완료 (2026-07-15)
  [Source: [architecture#new-components](../architecture/self-service-ai-assistant-architecture.md) — `settings_catalog.py`]
  - [x] `dataclass DomainEntry(name, get_fn, schema, destructive)` 정의
  - [x] `_CATALOG: dict[str, DomainEntry]` 모듈 레벨 레지스트리
  - [x] `list_domains() -> list[str]`
  - [x] `get_domain_schema(domain: str) -> dict` — 미등록 도메인이면 빈 dict + 로그 경고
  - [x] `get_domain_value(domain: str, owner: str) -> dict` — 등록된 `get_fn(owner)` 호출(sync/async 모두 지원, `inspect.isawaitable`로 판별), 예외는 `{"error": ...}` 반환

- [x] **Task 2: 7개 도메인 등록** (AC: 1) — ✅ 완료 (2026-07-15)
  - [x] Task 0에서 확정한 함수로 각 도메인을 `_CATALOG`에 등록
  - [x] `destructive` 기본값은 `True`(안전측 실패) — `persona`와 `contacts`만 명시적으로 안전하다고 판단해 `False`로 지정(단순 열람, 생성/수정/삭제 없음)

- [x] **Task 3: 카탈로그 커버리지 테스트** (AC: IV2) — ✅ 완료 (2026-07-15)
  - [x] 7개 도메인 각각 최소 1개 필드 조회 왕복 테스트 + 미등록/예외흡수/스키마 테스트 포함 14건 전체 통과

## Dev Notes

### Previous Story Insights

Story 1.1~1.3은 감지·라우팅·매뉴얼 RAG를 다뤘다. 본 Story부터는 **자동설정/조회 계열 기능의 기반**을 만든다 — 이후 Story 1.5(온보딩), 1.6(조회 Tool), 1.8(자동설정)이 모두 이 카탈로그 위에 쌓인다.

### 신규 설정 도메인 등록 규칙 (향후 유지보수 원칙)

신규 설정 페이지/필드가 추가될 때 `_CATALOG`에 등록하지 않으면 AI가 해당 항목을 전혀 인식하지 못한다(FR11). 이 규칙을 모듈 docstring에 명시한다.
[Source: [architecture#coding-standards](../architecture/self-service-ai-assistant-architecture.md)]

### File Locations

```
src/ai_voicebot/self_service/settings_catalog.py                    # 신규(구현 완료)
tests_new/unit/test_ai_voicebot/test_settings_catalog.py            # 신규(구현 완료, 프로젝트 실제 컨벤션 경로로 배치)
```
[Source: [architecture#source-tree](../architecture/self-service-ai-assistant-architecture.md)]

### Technical Constraints

- **NFR4 관련**: 이 Story에서는 `destructive` 플래그만 등록하고 실제 제외 로직 집행은 Story 1.8에서 구현한다(관심사 분리).
- 각 도메인의 `get_fn`은 **반드시 `owner` 파라미터를 받아야 한다** — 다른 테넌트 데이터 유출 방지(§Architecture Security Integration 원칙).

## Testing

### Testing Standards

- **Test file location**: `tests_new/unit/test_ai_voicebot/test_settings_catalog.py`(프로젝트 실제 컨벤션인 `tests_new/unit/test_<area>/test_<name>.py` 패키지 구조 적용, Story 1.1~1.3과 동일 위치)
- **Framework**: pytest
- **필수 테스트 케이스**:
  1. `list_domains()`가 정확히 7개 도메인명을 반환
  2. 7개 도메인 각각 `get_domain_value(domain, owner)`가 실제 값을 반환(왕복 테스트, IV2)
  3. 미등록 도메인 조회 시 오류 반환(AC3)
  4. `get_fn`이 예외를 던지면 `{"error": ...}` 형태로 흡수되어 반환됨(부작용 없음 확인, IV1)

## Change Log

| Date       | Version | Description                                                                                                                                                                                             | Author                  |
| ---------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 2026-07-14 | 0.1     | 최초 Draft 생성 — 시퀀싱 정정으로 구 Story 1.7에서 읽기 전용 부분 분리                                                                                                                                  | Copilot (BMAD SM 역할)  |
| 2026-07-15 | 0.2     | Task 0~3 구현 완료 — 미확인 4개 도메인(ai-escalation/contacts/general/integrations) 백엔드 함수 확정, `settings_catalog.py` 신규 작성(7개 도메인 등록), 커버리지 테스트 14건 전체 통과. Status → Review | Copilot (BMAD Dev 역할) |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 4.5 (GitHub Copilot)

### Debug Log References

- `python -m pytest tests_new/unit/test_ai_voicebot/test_settings_catalog.py -v --no-cov` → 14 passed
- `python -m pytest tests_new/unit/test_ai_voicebot tests_new/unit/test_events -q --no-cov` → 74 passed, 회귀 없음

### Completion Notes List

- Task 0: Architecture가 미확인 처리한 4개 도메인(ai-escalation/contacts/general/integrations)을 실제 코드 조사로 확정. 특히 `ai-escalation`은 별도 서비스가 아니라 persona 객체의 `escalation_mode` 필드임을 프론트엔드 코드(`ai-escalation/page.tsx`)로 확인.
- Task 0: 프론트엔드 네비게이션 조사 중 `integrations`→`general`, `contacts`(settings)→`/contacts` 리다이렉트를 발견 — 실제 UI는 통합되어 있으나, 카탈로그 관점에서는 자동설정 시 destructive 판단이 다를 수 있는 서로 다른 개념이므로 AC1이 명시한 7개 도메인을 그대로 유지하기로 판단.
- Task 1~2: `call-control` 도메인은 Task 0 권장안대로 라우터 함수가 아닌 `src/call_control/db.py` 데이터 계층을 직접 호출(FastAPI `Query(...)` 의존성 우회). 모든 `get_fn`을 `async def`로 통일해 `get_domain_value`가 sync/async 분기 없이 그냥 await하도록 단순화(`persona`만 실제 async I/O, 나머지는 내부적으로 sync 호출이지만 인터페이스 통일).
- Task 3: `general` 도메인은 `TENANTS_DATA`가 정적 하드코딩 데이터라 I/O 모킹 없이 실제 값으로 왕복 검증(다른 6개 도메인은 하위 I/O 함수를 monkeypatch).

### File List

- `src/ai_voicebot/self_service/settings_catalog.py` (신규)
- `tests_new/unit/test_ai_voicebot/test_settings_catalog.py` (신규, 14 tests)

## QA Results

**검토일**: 2026-07-15 | **검토자**: Copilot (BMAD PO/QA 역할 겸임, Epic 레벨 통합 검토)

AC/IV 전체 코드·테스트 대조 완료. 상세 근거는 Epic 레벨 통합 리포트 참고:
[2026-07-15_self_service_epic1_po_qa_review.md](../reports/2026-07/2026-07-15_self_service_epic1_po_qa_review.md)

- ✅ 승인(PASS) — 회귀 테스트 포함 전체 통과, 추가 조치 없음
