# Story 1.46: 지식베이스 ↔ 응대이력 교차 탐색 + 문구 번역 계층

## Status
Review

> 상세 배경(사용자가 지적한 raw hop 문자열·번호 나열 UI 사례): [Epic 4 계획 리포트](../reports/2026-08/2026-08-06_epic4_platform_maturation_and_ux_transparency_planning.md) §2.3/§2.4, §5(FR35-F).

## Story
As a 테넌트 관리자,
I want 지식베이스 항목(매뉴얼 Q&A/설정/업로드 문서)에서 "이게 실제로 어떤 대화에 쓰였는지"를
바로 확인하고, hop 경로·응대 이력이 개발자용 원시 텍스트가 아니라 사람이 읽는 문구로 보이기를,
so that AI 응대가 실제로 내 데이터에 기반한다는 것을 체감하고 신뢰할 수 있다.

## Acceptance Criteria
1. `qa`/`catalog`/`kb` 탭의 각 항목에 "최근 이 항목이 쓰인 대화 보기" 링크가 있다.
2. 해당 링크는 Story 1.38 세션 목록 API를 `related_domain`/`doc_id` 필터로 호출해 관련 세션만
   보여준다(신규 쿼리 로직 최소화, 기존 API에 선택적 필터 파라미터만 추가).
3. hop 경로 원시 문자열(`--rendered_by(hop1)-->` 형식)이 사용자에게 보이는 화면에서는 화살표
   카드/아이콘 형태로 렌더링된다(Story 1.32 그래프 컴포넌트 스타일 재사용).
4. 세션 flowchart의 "① 유형 ② RAG ③ hop ④ Tool ⑤ 응답" 번호 나열이 한 줄 자연어 요약 카드로
   대체된다(예: "🔍 지식 2건을 참고해 F(모호성 해소) 방식으로 1.75초만에 답했어요").
5. 유형 C(하이브리드)의 여러 도메인 검색 결과는 "hybrid" 등 기술 용어 없이 "여러 영역을 동시에
   참고했다"는 문구로 표시된다(Story 1.45와 연계).

## Tasks / Subtasks
- [x] Task 1: `GET /decision-log/sessions`에 선택적 `related_domain`/`doc_id` 필터 파라미터 추가
      (`self_service.py::_session_matches_filter`, 세션 상세를 재조회해 턴 단위 RAG 매칭 여부로 필터링)
- [x] Task 2: `qa`/`catalog`/`kb` 카드에 "최근 이 항목이 쓰인 대화 보기" 링크 추가(클릭 시
      policy 탭으로 이동 + 필터 배너 표시)
- [x] Task 3: hop 원시 문자열 → 카드 UI 변환 컴포넌트(`HopPathTrail`, `page.tsx` 내 공유
      컴포넌트 — Story 1.40 지식베이스 사례 카드와 Story 1.37 kb 문서 hop 토글 둘 다 적용)
- [x] Task 4: 세션 flowchart 턴 카드에 자연어 요약 한 줄 추가(`summarizeTurnPlain()`, 기존 ①②③④⑤
      배지는 상세 확인용으로 그대로 유지)
- [x] Task 5: `tsc --noEmit`/`eslint` 검증(프론트엔드 테스트 런더 미설치 — 기존 Story 관례와 동일),
      백엔드는 `tests_new/unit -k "self_service or intellidecision"` 328건 전체 회귀 통과
- [ ] Task 6: 실서버 IV(교차 탐색 링크 클릭 → 실제 관련 세션만 필터링되어 표시되는지 확인)

## Dev Notes
- 문구 번역은 프론트엔드 전용 표시 계층으로만 처리하고 백엔드 로그/API의 원시 데이터는 그대로
  유지한다(디버깅 목적의 raw 데이터 접근성은 보존).

## Dev Agent Record
- 백엔드(`self_service.py`): `GET /decision-log/sessions`에 `related_domain`/`doc_id` 선택적
  쿼리 파라미터 추가 — 주어지면 후보 세션(네단로 5배 그리거나 200개까지)를 각각
  `get_decision_log_session_detail()`로 재조회해 턴별 RAG 매칭(`matched_doc_ids`/`related_domains`)이
  일치하는 세션만 남긴다(`_session_matches_filter`). 신규 저장 로직 없음, 기존 조회 경로만 재사용.
- 프론트엔드(`page.tsx`): `handleViewSessionsFor()` 신설 — qa 항목은 `doc_id`, catalog/kb는
  `related_domain`으로 필터를 설정하고 `policy` 탭으로 이동. 필터 배너 + "필터 해제" 버튼 추가.
- `HopPathTrail` 공유 컴포넌트 신설(`humanizeHopNode`/`HOP_EDGE_LABEL` 함께) — Story 1.40 지식베이스
  사례 카드와 Story 1.37 kb 문서 hop 토글 둘 다에서 원시 `--edge_type(hopN)-->` 문자열을 대체.
- `summarizeTurnPlain()` 신설 — 세션 flowchart 턴 카드 상단에 자연어 한 줄 요약을 추가하고, 기존
  ①②③④⑤ 배지는 상세 확인용으로 그대로 유지(정보 손실 없음).
- `tsc --noEmit`/`eslint` 0에러. 백엔드 새 라우터 등록 확인(`GET /decision-log/sessions` 등) +
  `tests_new/unit -k "self_service or intellidecision"` 328건 전체 회귀 통과.
- **잔여**: 실서버 IV(Task 6) — 서버 재시작 필요, 사용자 승인 대기.

## QA Results
(대기)

*최종 업데이트: 2026-08-06*
