# Story 2.8: 매뉴얼-도메인 매핑 동적화 (선택, 낮은 우선순위)

## Status

Done

## Story

**As a** 개발자,
**I want** `manual_indexer.py`의 하드코딩된 섹션 키워드 매칭 리스트를 매뉴얼 문서 자체의 명시적 태그로 대체하기를,
**so that** 매뉴얼 작성자가 코드를 몰라도 정확한 도메인 연결을 보장할 수 있다.

## Acceptance Criteria

1. `self-service-manual-content.md`의 각 섹션 제목에 명시적 도메인 태그(예: `## 3. AI 에스컬레이션 설정 {domain: ai-escalation}`)를 추가하는 컨벤션을 정의한다.
2. `manual_indexer.py`가 이 태그를 우선 사용하고, 태그가 없으면 기존 키워드 매칭으로 폴백한다(점진적 마이그레이션 허용, 매뉴얼 전체를 한 번에 바꾸지 않아도 됨).

### Integration Verification

- **IV1**: 태그 도입 후 기존 매뉴얼 색인·RAG 검색 결과(Story 1.3 QA 케이스)가 회귀 없이 동일하다.

## Tasks / Subtasks

- [x] **Task 1**: 태그 컨벤션 정의 및 매뉴얼 문서 갱신 — `## N. 제목 {domain: 도메인명}` 형식으로 `self-service-manual-content.md`의 9개 섹션(1~9) 모두에 명시적 태그 추가(부록은 도메인 없으므로 미적용).
- [x] **Task 2**: `manual_indexer.py`의 `_SECTION_PATTERN`에 선택적 `domain_tag` 그룹 추가, `parse_manual_qa_with_meta()`가 태그를 우선 사용하도록 수정.
- [x] **Task 3**: 폴백 경로(태그 없는 섹션은 기존 키워드 매칭) 유지 확인 — 단위테스트 5개로 태그 있음/없음/충돌/혼합 케이스 모두 검증.
- [x] **Task 4**: Story 1.3 QA 케이스 재실행 — 실제 매뉴얼 파일을 직접 파싱해 기존과 동일하게 52건 Q&A 전체가 추출되고 9개 섹션 모두 올바른 도메인으로 매핑되며 섹션 제목에서 태그가 깔끔하게 제거됨을 확인. 단위회귀 전체 통과.

## Dev Notes

**우선순위 낮음**: 사용자 요청(하드코딩 전수 점검)에 포함되어 계획에는 넣었으나, Epic 2의 핵심
가치(카탈로그/Screen Graph 동적화, IntelliDecision 개선)와 비교하면 영향도가 작다. Epic 2의
나머지 Story가 완료된 뒤 여유가 있을 때 착수를 권장한다.

## Change Log

| Change    | Date       | Version | Description                                                          | Author                           |
| --------- | ---------- | ------- | -------------------------------------------------------------------- | -------------------------------- |
| 초안 생성 | 2026-07-20 | 0.1     | Epic 2 Story 초안 작성(사용자 요청의 "모든 하드코딩 검토" 범위 반영) | Copilot (BMAD PM/Architect 역할) |  | 구현 완료 | 2026-07-21 | 1.0 | 태그 컨벤션 도입 + 매뉴얼 9개 섹션 태그화 + `manual_indexer.py` 태그 우선 인식(폴백 유지) + 단위테스트 5개 + 실제 매뉴얼 파일 검증(52건 그대로 파싱). Status → Done | Copilot (Dev 역할) |
## Dev Agent Record

### Agent Model Used

Claude Sonnet 4.5 (GitHub Copilot)

### Completion Notes

- 태그 형식을 `{domain: xxx}`로 정하고 섹션 제목 끝에 붙이는 방식으로 설계해, 기존 "## N. 제목" 패턴과 자연스럽게 공존하면서도 파싱 시 태그가 표시용 제목에서는 자동으로 제거되도록 구현(사용자에게/프론트엔드에 태그 문자열이 노출되지 않음).
- 실제 매뉴얼 문서(`self-service-manual-content.md`) 9개 섹션(부록 제외) 모두에 태그를 추가했다 — 기존 `_SECTION_TO_DOMAIN` 키워드 매칭과 동일한 도메인값을 그대로 사용해, 이미 ChromaDB에 색인된 기존 데이터(키워드 매칭으로 생성된 related_domain)와 값이 일치해 재색인 없이도 일관성이 깨지지 않도록 했다.
- 실제 `load_manual_qa_with_meta()`를 직접 호출해 52건 Q&A가 모두 그대로 추출되고(회귀 없음) 9개 섹션 모두 올바른 도메인으로 매핑됨을 실제 파일에 대해 검증해 IV1을 충족했다.

### File List

- `src/ai_voicebot/self_service/manual_indexer.py` (수정 — `_SECTION_PATTERN`에 `domain_tag` 그룹 추가, `parse_manual_qa_with_meta()` 태그 우선 로직)
- `docs/product/self-service-manual-content.md` (수정 — 9개 섹션 제목에 `{domain: xxx}` 태그 추가)
- `tests_new/unit/test_ai_voicebot/test_self_service_manual_rag.py` (수정 — `TestParseManualQaWithMetaDomainTag` 클래스 5개 테스트 추가)

## QA Results

**단위 테스트**: `pytest tests_new/unit/test_ai_voicebot/test_self_service_manual_rag.py` → 22개 통과(신규 5개 포함). 전체 회귀 통과(회귀 0건).

**실제 매뉴얼 파일 검증**: `load_manual_qa_with_meta()`로 직접 파싱해 52건 Q&A 전체 추출 확인(태그 도입 전과 동일), 9개 섹션 모두 올바른 도메인(`intro`/`onboarding`/`ai-escalation`/`operator-status`/`call-control`/`booking`/`chat-relay`/`call-history`/`self-service`)로 매핑되고 섹션 제목에서 태그가 깔끔하게 제거됨을 확인.

IV1: 기존 키워드 매칭과 동일한 도메인값을 사용해 태그 도입이 기존 색인 데이터와 충돌하지 않음(재색인 불필요). Story 1.3 QA 회귀 충족.

Gate 판정: **PASS**.
