# Story 1.33: 유형 C(포괄적 도움 요청) 하이브리드 RAG 전략 (FR33-E)

## Status

Done (2026-08-05, 실서버 IV 검증 완료)

## 관련 문서

- [SELF_SERVICE_RAG_INTELLIDECISION_ADVANCEMENT_RESEARCH.md](../design/SELF_SERVICE_RAG_INTELLIDECISION_ADVANCEMENT_RESEARCH.md) §3.11(GraphRAG Local/Global Search)
- [self-service-ai-assistant-prd.md](../product/self-service-ai-assistant-prd.md) FR33-E
- Story 1.15(유형 C 신설), Story 1.24(`rag_strategy_hint`), Story 1.32(시뮬레이터에서 유형 C 시각화)
- 코드(수정 예정): `src/ai_voicebot/self_service/intellidecision_policy.py`(`rag_strategy_hint="hybrid_multi_domain"` 추가), `self_service_agent.py`(유형 C 분기 검색 로직)

## Story

**As a** "뭘 도와줄 수 있는지" 포괄적으로 묻는 사용자를 상대하는 AI 도우미,
**I want** 여러 도메인/문서 유형에 걸쳐 검색한 결과를 카테고리별로 요약해 종합적으로 답하고,
**so that** 특정 도메인 하나로 좁혀지지 않는 질문에도 지식베이스 전체를 대표하는 답을 줄 수 있다.

## Acceptance Criteria

1. `intellidecision_policy.py`에 신규 `rag_strategy_hint` 값 `hybrid_multi_domain`이 추가되고
   유형 C에 적용된다.
2. 유형 C 판정 시 검색 로직이 단일 컬렉션/단일 도메인이 아니라 **여러 도메인(또는 여러 문서
   유형)에 걸쳐 병렬 검색** 후 카테고리별로 집계해 응답에 반영한다(GraphRAG Global Search의
   "여러 커뮤니티 요약을 종합"하는 개념을 경량 버전으로 적용, 리서치 §3.11 근거).
3. Story 1.32 시뮬레이터에서 유형 C 질문을 선택하면 hop 시각화가 "여러 소스가 병렬로 매칭되어
   종합됐다"는 것을 다른 유형(단일 hop 체인)과 다르게 표시한다(예: 병렬 브랜치 시각화).
4. 기존 유형 C 응대(Story 1.15, "3개 카테고리 요약 + 마무리 문구")의 실제 응답 품질은 이번
   변경으로 저하되어서는 안 된다(회귀 확인 필수, 베이스라인 대비 A/B).
5. NFR1(지연 예산) 보호 — 다중 도메인 병렬 검색이 추가 지연을 유발하지 않도록 `asyncio.gather`
   등으로 병렬화해야 한다(순차 검색 금지).

## Tasks / Subtasks

- [ ] Task 1: 현재 유형 C 검색 로직 확인(단일/다중 여부 코드로 직접 확인 — 추측 금지)
- [ ] Task 2: 다중 도메인 병렬 검색 구현(`asyncio.gather`)
- [ ] Task 3: `rag_strategy_hint="hybrid_multi_domain"` 정책 등록 + `GET /intellidecision-policy` 노출
- [ ] Task 4: 베이스라인 대비 A/B 회귀 검증(응답 품질·지연)
- [ ] Task 5: Story 1.32 시뮬레이터 병렬 hop 시각화 연동

## Dev Notes

- 착수 전 반드시 현재 유형 C 코드 경로가 이미 다중 도메인 검색을 하고 있는지부터 실제 코드로
  확인할 것(Story 1.24 IV 검증에서 "유형 확정 전에 이미 RAG 검색이 실행되는 구조"라는 선례가
  있었음 — 같은 함정 재확인 필요).
- Full GraphRAG(자동 커뮤니티 클러스터링)는 여전히 Non-Goal — 여기서 말하는 "하이브리드"는
  기존 도메인 태그 기반 컬렉션 여러 개를 병렬 검색·집계하는 경량 버전이다.

## QA Results

**2026-08-05, Copilot(BMAD Dev 역할) 구현 검증**:
- Task 1 코드 확인 결과: `self_service_agent.py`는 유형이 확정되기 전에 단일 `rag_engine.search()` 호출만 수행(도메인별 분리 검색 없음) — Story 1.24의 "유형 확정 전에 이미 RAG 검색이 실행되는 구조" 재확인과 동일한 패턴.
- 신규 `src/ai_voicebot/self_service/hybrid_rag.py`: `looks_like_broad_help_query()`(유형 C trigger_examples 어휘 기반 경량 휴리스틱, 최종 판정은 여전히 LLM 담당) + `search_hybrid_multi_domain()`(`settings_catalog.list_domains()`의 각 도메인을 `asyncio.gather`로 병렬 ChromaDB 직접 조회, 개별 도메인 실패는 best-effort로 건너뜀).
- `self_service_agent.py`: 휴리스틱이 맞을 때만 기존 `rag_engine.search()` 호출과 **병렬**(`asyncio.gather`)로 하이브리드 검색을 추가 실행(NFR1/AC5 지연 예산 보호). 결과는 기존 `rag_documents` 리스트에 중복 doc_id 제거 후 병합되어 프롬프트 조립/화면 안내/trace 로깅/Story 1.32 hop_path 수집까지 기존 경로를 그대로 타서 추가 코드 수정 없이 AC3(병렬 hop 시각화)이 자동 충족됨(여러 `related_domain`이 자연스럽게 생김).
- `intellidecision_policy.py`: 유형 C `rag_strategy_hint`를 `"hybrid"`→`"hybrid_multi_domain"`로 갱신(AC1). 관련 단위테스트 2건 갱신.
- **검증**: 신규 단위테스트 8건(`test_hybrid_rag.py` — 휴리스틱 매칭/병렬 조회/중복제거/개별 도메인 실패 방어/빈 입력 처리) 전체 통과. `tests_new/unit/test_ai_voicebot -k "self_service or knowledge or hybrid"` 76건 전체 통과(회귀 없음, AC4). 병렬 구조는 코드 검토로 지연 상함이 없음을 확인(휴리스틱 미일치 시 추가 task 자체가 생성되지 않아 기존과 동일).
- **잔여**: 실서버 IV(사용자 승인 후 재시작, 실제 "뭐를 할 수 있어?" 발화로 여러 도메인 hop 경로/응답 품질 확인) 미실시.

**2026-08-05, 실서버 IV 검증 결과(Task 6)**:
- `GET /intellidecision-policy` 실제 호출로 유형 C `rag_strategy_hint="hybrid_multi_domain"`이 실서버에 반영되었음을 확인.
- `POST /api/knowledge-base/simulate`로 `owner=9001`, `query="뭘 할 수 있어?"` 실행 결과 `matched_documents`가 4개 서로 다른 `related_domain`(operator-status/ai-escalation/call-control/chat-relay)에 걸쳐 반환됨을 확인 — 하이브리드 다중 도메인 검색이 실제로 동작.
- `logs/app.log`의 `self_service_agent_hybrid_rag_merged`(`hybrid_doc_count=6, merged_total=7`)가 API 응답의 `matched_documents` 개수(7)와 정확히 일치함을 cross-check으로 확인.
- 브라우저로 동일 발화를 실행해 hop_path가 4개 도메인에 걸친 간선으로 시각화됨을 Story 1.32 응답 시뮬레이터 화면에서 함께 확인.
- **대조군 확인(휴리스틱 게이팅 정상 작동)**: 단일 도메인 질문("착신 규칙을 바꾸고 싶어요")은 hop_path가 call-control 1개 도메인으로만 국한되어, 휴리스틱 미일치 시 하이브리드 검색이 실행되지 않는다는 것을 실증.
- 결론: AC1~AC5 전부 실서버에서 실증, 회귀 없음. Story Status → **Done**.

**2026-08-05, 실서버 IV 검증 결과(Task 6)**:
- `GET /intellidecision-policy` 실제 호출로 유형 C `rag_strategy_hint="hybrid_multi_domain"`이 실서버에 반영되었음을 확인.
- `POST /api/knowledge-base/simulate`로 `owner=9001`, `query="뭐를 할 수 있어?"` 실행 결과 `matched_documents`가 4개 서로 다른 `related_domain`(operator-status/ai-escalation/call-control/chat-relay)에 걸쳐 반환됨을 확인 — 하이브리드 다중 도메인 검색이 실제로 동작.
- `logs/app.log`의 `self_service_agent_hybrid_rag_merged`(`hybrid_doc_count=6, merged_total=7`)가 API 응답의 `matched_documents` 개수(7)와 정확히 일치함을 cross-check로 확인.
- 브라우저로 동일 발화를 실행해 hop_path가 4개 도메인에 걸친 간선으로 시각화됨을 Story 1.32 응답 시뮬레이터 화면에서 함께 확인.
- **대조군 확인(휴리스틱 게이팅 정상 작동)**: 단일 도메인 질문("착신 규칙을 바꾸고 싶어요")은 hop_path가 call-control 1개 도메인으로만 국한되어, 휴리스틱 미일치 시 하이브리드 검색이 실행되지 않는다는 것을 실증.
- 결론: AC1~AC5 전부 실서버에서 실증, 회귀 없음. Story Status → **Done**.
