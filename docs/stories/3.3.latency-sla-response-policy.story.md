# Story 3.3: 5초 초과 시 대응 정책 결정 및 구현

## Status

Done(2026-07-29 사용자 승인 대로 "대기 멘트/HITL 즉시 폴백 없이 로깅만 남겨 후속 디버깅에 활용"으로 정책 확정, 기존 Story 3.2 구현으로 충족 확인)

> 참조원: [voice-latency-turn-taking-prd.md](../product/voice-latency-turn-taking-prd.md) FR5 / Open Question 1

## Story

**As a** 발신자(고객),
**I want** AI 응답이 오래 걸릴 때 침묵 대신 적절한 안내를 받고 싶다,
**so that** 전화가 끊겼다고 오인하거나 불필요하게 재질문하지 않는다.

## Acceptance Criteria

1. Story 3.2의 `response_latency_sla_exceeded` 이벤트 발생 빈도/원인 분포 데이터를 최소 1~2주(또는
   QA 샘플 수십 건) 수집한다.
2. 수집 데이터를 근거로 정책(예: "N초 초과 시 대기 멘트 삽입" 또는 "즉시 HITL 폴백")을 결정하고
   사용자/PO 승인을 받는다(Open Question 1, PRD 승인 전 확정 필요 항목).
3. 승인된 정책이 코드로 반영되고, 정책 발동 여부가 `call_data_record`에 로깅된다.
4. 정책 발동이 정상 케이스(5초 이내)의 응답 흐름에는 영향을 주지 않는다.

## Tasks / Subtasks

- [x] **Task 1: 데이터 수집 및 분포 분석** (AC: 1) — 실제 운영 로그(`call_data_record_*.log`)에
  `response_latency_sla_exceeded` 이벤트가 0건임을 확인(Epic 6 thinking 비활성화 이후 평균 응답
  지연이 2초대로 개선되어 5초 초과가 자연 발생하지 않는 상태). 대신 5개 대표 실패 모드(RAG
  지연/Gemini 빈 candidate 재시도 누적/LLM 전체 생성 지연/TTS 합성 지연/post_llm_processing 지연)를
  합성 breakdown으로 구성해 `suspected_sla_stage()`가 매번 정확히 병목 구간을 지목함을 확인(5/5 PASS,
  추가 구현 없이 기존 순수함수 검증).
- [x] **Task 2: 정책안 작성 및 승인** (AC: 2) — pre-TTS 대기멘트 삽입 안을 제안했으나, 사용자가
  **"대기멘트 없이 로깅을 남겨서 나중에 디버깅"** 으로 최종 승인(2026-07-29) — 실시간 개입(대기
  멘트/HITL 즉시 폴백)은 **도입하지 않기로 확정**.
- [x] **Task 3: 정책 구현** (AC: 3, 4) — 결정된 정책이 "패시브 로깅만"이므로 신규 개입 코드가 필요
  없음 — Story 3.2의 `check_and_tag_sla_exceeded()`가 이미 `call_id`/`turn_id`/`total_ms`/
  `threshold_ms`/`suspected_stage`/`stage_breakdown_ms`/`user_text_preview`를 `call_data_record`에
  남기고 있어 AC3 요건을 이미 충족함을 코드 재확인.
- [x] **Task 4: 회귀 테스트** (AC: 4) — 실시간 개입 코드가 없으므로 정상 케이스(5초 이내) 응답
  흐름에 영향 자체가 구조적으로 불가능(AC4 자동 충족). 기존 `tests_new/unit`의
  `ai_response_latency_compare` 관련 테스트가 그대로 유효함을 확인.

## Dev Notes

- 이 Story는 **데이터 확보 후에 착수**한다 — Story 3.1/3.2 배포 없이 선행 불가.
- 성급한 결론 금지: self-service 트랙에서 반복 확인된 교훈("작은 표본으로 결론 내리지 말 것")을
  동일 적용한다.

## 사전 QA 테스트 계획(2026-07-27 작성, **실행은 보류** — 데이터 확보 후 일괄 수행)

### 리스크 프로파일(간이)

| 리스크                                            | 확률 | 영향 | 비고                                               |
| ------------------------------------------------- | ---- | ---- | -------------------------------------------------- |
| 정책 오적용(정상 응답에도 대기 멘트 삽입)         | 중   | 중   | AC4의 "정상 케이스 무영향" 요건으로 방어           |
| 데이터 부족으로 성급한 정책 결정                  | 중   | 높음 | Dev Notes의 "성급한 결론 금지" 원칙으로 방어       |
| 대기 멘트가 TTS 파이프라인 구조와 충돌(이중 재생) | 낮음 | 높음 | Epic 4 설계(Story 4.1/4.2)와 상호 의존성 확인 필요 |

### Given-When-Then 시나리오

| #   | Given                                          | When                    | Then                                                               |
| --- | ---------------------------------------------- | ----------------------- | ------------------------------------------------------------------ |
| 1   | Story 3.2 데이터(1~2주 또는 QA 샘플) 확보 완료 | 분포 분석               | 5초 초과 빈도·원인별 비중이 정량화되어 정책안(AC2) 초안이 작성됨   |
| 2   | 정책안 초안 작성 완료                          | 사용자/PO에게 제시      | 승인 또는 보완 요청 피드백 수신(Open Question 1 해소)              |
| 3   | 승인된 정책(예: N초 초과 시 대기 멘트)         | 5초 초과 통화 재현      | 정책 발동이 `call_data_record`에 로깅되고, 실제 대기 멘트가 재생됨 |
| 4   | 동일 정책 적용 상태                            | 5초 이내 정상 통화 재현 | 정책이 발동하지 않고 기존 응답 흐름이 그대로 유지됨(AC4 회귀 방지) |

이 테스트 계획은 Story 3.2의 데이터 수집이 완료된 이후에만 실행 가능하다(순서 의존성, 이번에는
계획만 작성하고 실행하지 않았다).

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                | Author                     |
| ---------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| 2026-07-24 | 0.1     | 초안 작성                                                                                                                                                                                                                  | GitHub Copilot             |
| 2026-07-29 | 1.0     | 사용자가 "대기멘트 없이 로깅만 남겨서 디버깅에 활용"을 정책으로 승인 — 실시간 개입 없이 기존 Story 3.2 로깅이 AC3/AC4를 이미 충족함을 확인, 합성 5개 시나리오로 `suspected_sla_stage()` 정확도 검증(5/5 PASS), Status→Done | Copilot (BMAD Dev/QA 역할) |

## Dev Agent Record

- 이 Story는 신규 코드를 추가하지 않는다 — 사용자가 선택한 정책(대기멘트/HITL 폴백 없는
  패시브 로깅만)은 Story 3.2(`check_and_tag_sla_exceeded`)가 이미 구현해둔 범위로
  충분하다. 실제 운영 로그에 사례가 없어(Epic 6 개선 이후 평균 응답 2초대) 합성 5개
  시나리오로 원인 판별 로직만 검증했다(임시 스크립트로 검증 후 삭제 — 생산 코드 미변경).
- **위험 프로파일 재확인**: "대기 멘트가 TTS 파이프라인 구조와 충돌(이중 재생)" 리스크가 사용자의
  "로깅만" 결정으로 자동 해소되었다 — 실시간 개입 자체가 없으므로 Epic 4(TTFT) 구현과의
  상호의존성도 사라졌다.

## QA Results

**단위 검증(2026-07-29)**: `suspected_sla_stage()` 순수함수에 5개 대표 실패 모드 합성 breakdown을
입력해 매번 정확한 병목 구간을 지목함을 확인(5/5 PASS). AC3/AC4는 Story 3.2 구현이 그대로
충족(신규 코드 없음 → 정상 케이스 회귀 위험 없음).

- ✅ 승인(PASS) — 정책 결정 사항으로 Story 종료, 추가 실서버 배포 필요 없음(코드 변경 없음)
