# Story 1.37: 지식베이스 API/Tool 단위 상세 카드 + hop 뷰 (FR34-C)

## Status

Done

## 관련 문서

- [self-service-ai-assistant-prd.md](../product/self-service-ai-assistant-prd.md) FR34-C
- [1.23.knowledge-base-inventory-transparency.story.md](1.23.knowledge-base-inventory-transparency.story.md), [1.31.upload-driven-knowledge-base-auto-assembly.story.md](1.31.upload-driven-knowledge-base-auto-assembly.story.md) — 선행 Story(현황 집계 API)
- [1.28.knowledge-graph-n-hop-generalization.story.md](1.28.knowledge-graph-n-hop-generalization.story.md) — 선행 Story(`traverse_graph()`)

## Story

**As a** 지식베이스 구성을 검증하려는 테넌트 관리자,
**I want** 업로드된 API(Tool)가 엔드포인트 하나하나 단위로 자세한 정보(경로/메서드/파라미터/
writable 여부)와 관련된 지식 그래프 hop 경로까지 확인 가능하기를,
**so that** "내 데이터가 어떻게 지식베이스로 구성됐는지" 투명하게 신뢰할 수 있다.

## Acceptance Criteria

1. "지식베이스" 섹션(Story 1.36 IA 재편 이후)에 API/Tool 단위 상세 카드 목록이 표시된다 — 각
   카드는 엔드포인트 경로·HTTP 메서드·writable 여부(Story 1.31 `classify_setting_item` 결과)·
   실행 승인 상태(Story 1.34/1.35, 있는 경우)를 보여준다.
2. 카드를 클릭하면 해당 API/문서와 연결된 `knowledge_graph` hop 경로(Story 1.28
   `traverse_graph()`)가 시각화된다 — 어떤 도메인·화면·IntelliDecision 유형과 연결되는지 확인
   가능해야 한다.
3. 목록/카드는 신규 API 없이 기존 `GET /knowledge-base/inventory`(Story 1.23/1.31의
   `auto_assembled.setting_items`)와 `knowledge_graph.traverse_graph()`를 조합해 구성한다(신규
   백엔드 엔드포인트가 필요하면 최소 범위로 추가).

## Tasks / Subtasks

- [x] Task 1: 카드 UI 와이어프레임 — 업로드 문서별 제목/소스타입/domain_tags/승인 메서드 표시
- [x] Task 2: 카드 클릭 시 hop 뷰 연동 — Story 1.38 AC10 2단계 로딩 패턴 재사용(`handleToggleDocHop`)
- [x] Task 3: 최소 범위 신규 API — `GET /knowledge-base/documents/{id}/hop-path`
  (`knowledge_graph.traverse_graph()` 재사용, domain_tags 시작점)
- [x] Task 4: 정적 검증 + 실서버 IV

## Dev Notes

- Story 1.32에서 이미 hop 경로를 칩(chip) 형태로 시각화하는 컴포넌트를 만들어뒀으므로, 이를
  최대한 재사용할 것(중복 컴포넌트 생성 지양).

## QA Results

### 코드 구현 완료(2026-08-06)

- `GET /api/settings/ai-assistant/knowledge-base/documents/{id}/hop-path` 신규 —
  `get_document()`로 실제 DB 업로드 문서를 조회한 후 `domain_tags`를 `traverse_graph()`의
  시작점으로 hop 경로를 산출(LLM 호출 없음, Story 1.28 재사용). 라우터
  임포트로 신규 라우트 등록 확인(AC3).
- `KnowledgeDocumentItem`에 `approved_methods` 필드 추가(Story 1.34 승인 상태 노출),
  `DocumentHopEdge`/`DocumentHopPathResponse` 타입 신규.
- `kb` 탭에 "업로드 문서별 상또 카드" 섹션 추가 — 카드(story 1.38 hop 치 UI 패턴 재사용)클릭
  시 `handleToggleDocHop` 2단계 로딩(스토리 1.38의 `handleToggleSession` 재사용 패턴).
- `npx tsc --noEmit`/`npx eslint` 0에러. `tests_new/unit` 전체 재실행 — 기존에 알려진
  사전 결함(`test_call_session.py` 12건) 제외 신규 실패 없음.
- **잔여(Task 4)**: 실서버 IV는 실제 OpenAPI 업로드 문서에 domain_tags가 있는 경우에만
  검증 가능(hop 경로는 domain_tags에서 시작함) — 다음 세션으로 이관.

### 실서버 IV 검증 결과(2026-08-06)

- 가상 OpenAPI 문서("Demo Order API", domain_tags=demo-orders) 업로드 →
  `GET /knowledge-base/documents/{id}/hop-path` 실호출 결과 `catalog_domain:demo-orders`를
  시작점으로 `writable`(유형 A/C/F/H/I) + `relates_to`(document) 엣지 6건이 정상 반환됨
  확인(AC2/AC3).
- 브라우저(Playwright)로 "지식베이스 현황" 탭에 "업로드 문서별 상세 카드" 섹션이 실제
  문서(도메인 태그 배지 포함)로 정상 렌더링됨을 확인(AC1) → "hop 경로" 토글 클릭 →
  hop 칩 6개가 실제 응답과 동일하게 표시됨을 확인(AC2).
- **부수효과로 발견된 버그(Story 1.35 범위, 함께 수정)**: 문서 목록 API가 `approved_methods`를
  응답하지 않아 카드에 승인 배지가 노출되지 않는 문제를 발견 — `KnowledgeDocumentItem`/
  `_to_document_item()`에 필드 추가로 수정(Story 1.35 QA Results에 상세 기록).
- 테스트 문서는 검증 후 삭제해 운영 데이터에 영향 없음. Status: Review → **Done**.
