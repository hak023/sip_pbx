# Story 2.3: Screen Graph 동적화

## Status

Done

## Story

**As a** 개발자,
**I want** `screen_graph.py`도 Story 2.2와 동일한 패턴으로 DB 기반으로 전환하기를,
**so that** 화면 안내 정보도 코드 배포 없이 갱신할 수 있다.

## Acceptance Criteria

1. `get_screen_for_domain()`/`list_all_screens()`/`describe_screen_for_conversation()` 시그니처는 변경 없이 내부 구현만 DB 조회로 전환된다.
2. 캐시·무효화 메커니즘은 Story 2.2와 동일 원칙을 재사용한다(중복 구현 지양, 공통 헬퍼 고려).

### Integration Verification

- **IV1**: `self_service_screen_graph_hit` 이벤트·화면 안내 문구가 리팩터링 전후 동일하게 생성됨을 회귀 테스트로 확인한다.

## Tasks / Subtasks

- [x] **Task 1**: `screen_graph.py`의 3개 공개 함수(`get_screen_for_domain`/`list_all_screens`/`describe_screen_for_conversation`) 내부 구현을 `catalog_config_loader.py` 기반으로 교체. `_SCREEN_REGISTRY`(`_register_screen()` 호출 결과)는 삭제하지 않고 하드코딩 폴백으로 그대로 유지. Screen Graph는 실행 가능한 콜러블이 없는 순수 데이터라 settings_catalog.py와 달리 화이트리스트 레지스트리가 불필요.
- [x] **Task 2**: `nav_hint` 필드(2026-07-20 개선분)가 DB 이관 후에도 정확히 보존되는지 확인 — 실제 시드된 DB(Story 2.1 마이그레이션)에서 `_build_dynamic_screens()`로 역직렬화한 결과가 정적 `_SCREEN_REGISTRY`와 route/title/description/nav_hint/fields 모든 필드에서 100% 일치함을 스크립트로 직접 실행해 확인(6개 도메인 모두).
- [x] **Task 3**: 전체 self_service 회귀 테스트 실행 — 295개 전체 통과(신규 `test_screen_graph_dynamic_loader.py` 9개 포함). 기존 `test_self_service_screen_graph.py`는 `tests_new/unit/conftest.py`(Story 2.2에서 추가된 오토유즈 픽스처) 덕분에 수정 없이 통과.

## Dev Notes

Story 2.2와 동일한 리팩터링 원칙(외부 동작 불변)을 따른다. `nav_hint`(메인화면 클릭 경로,
2026-07-20 결함 개선 추가분)와 `route`(프론트 클릭 이동용) 두 필드 모두 DB 스키마에 포함되어야
한다 — 마이그레이션 누락 주의.

### File Locations

```
src/ai_voicebot/self_service/screen_graph.py   # 수정(내부 구현 교체)
```

## Change Log

| Change    | Date       | Version | Description            | Author                           |
| --------- | ---------- | ------- | ---------------------- | -------------------------------- |
| 초안 생성 | 2026-07-20 | 0.1     | Epic 2 Story 초안 작성 | Copilot (BMAD PM/Architect 역할) |  | 구현 완료 | 2026-07-20 | 1.0 | `screen_graph.py` 동적 로딩 전환(하드코딩 폴백 유지) + nav_hint 왕복 검증 + 테스트 추가. Status → Done | Copilot (Dev 역할) |
## Dev Agent Record

### Agent Model Used

Claude Sonnet 4.5 (GitHub Copilot)

### Completion Notes

- Story 2.2와 동일한 패턴(정적 레지스트리 폴백 + `id()` 기반 캐시)을 재사용해 구현. 콜러블이 없는 순수 데이터이므로 화이트리스트 검증 로직이 필요 없어 Story 2.2보다 단순함(`_build_dynamic_screens()`는 dict를 `ScreenEntry`/`UiFieldSpec`로 그대로 역직렬화할 뿐).
- Task 2(nav_hint 보존) 검증을 단순 단위 테스트뿐 아니라 **실제 시드된 개발 DB**에 대해 직접 실행하는 검증 스크립트로 확인함 — 6개 도메인 모두 route/title/description/nav_hint/fields가 정적 레지스트리와 1바이트도 다르지 않음을 확인(Story 2.1 마이그레이션 스크립트가 정확히 동작함을 간접 재확인하는 효과도 있음).
- Story 2.2에서 추가한 `tests_new/unit/conftest.py` 오토유즈 픽스처 덕분에 기존 `test_self_service_screen_graph.py`는 수정 없이 그대로 통과(IV1).

### File List

- `src/ai_voicebot/self_service/screen_graph.py` (수정 — `_get_effective_screens()`/`_build_dynamic_screens()` 추가, 3개 공개 함수가 이를 경유하도록 교체)
- `tests_new/unit/test_ai_voicebot/test_screen_graph_dynamic_loader.py` (신규, 9개 테스트)

## QA Results

**실행 일자**: 2026-07-20

- 전체 회귀: `pytest tests_new/unit/test_ai_voicebot tests_new/unit/test_events -q --no-cov` → **전체 통과**(295개).
- 동적 로딩 동작 검증(`test_screen_graph_dynamic_loader.py`, 9개): 폴백 3건, 동적 구성 적용·동적 nav_hint 반영 확인·미등록 도메인 None·캐시 항등성 6건.
- IV1(가장 중요): 실제 시드된 DB에서 직접 읽은 6개 도메인 전부가 route/title/description/**nav_hint**/fields 기준 정적 레지스트리와 1바이트도 다르지 않음을 실제 스크립트로 확인(Task 2). 기존 테스트 로직 수정 없이 회귀 0건.
- Gate 판정: **PASS**.
