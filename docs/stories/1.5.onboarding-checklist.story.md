# Story 1.5: 온보딩 체크리스트 안내

## Status

Review

## Story

**As a** 테넌트 관리자,
**I want** 아직 설정하지 않은 항목이 있으면 AI가 먼저 알려주기를,
**so that** 놓친 초기 설정 없이 서비스를 온전히 활용할 수 있다.

## Acceptance Criteria

1. `get_onboarding_checklist` Tool이 **설정 카탈로그(Story 1.4)의 조회 함수**를 사용해 미완료 항목(예: 페르소나 미등록, 지식베이스 미업로드, 알림 미설정)을 판별해 목록으로 반환한다.
2. 셀프서비스 세션 시작 시 미완료 항목이 있으면 AI가 먼저 안내하고, 사용자가 원하면 바로 해당 항목의 자동설정 Tool(Story 1.8)로 이어간다.
3. 모든 항목이 완료된 테넌트는 체크리스트 안내를 생략하고 일반 질의응답으로 진행한다.

### Integration Verification

- **IV1**: 체크리스트 판별 로직이 설정 카탈로그의 조회 함수만 사용하며 별도 완료 여부 저장소를 새로 만들지 않는다(단일 진실 소스 원칙).
- **IV2**: 체크리스트 안내가 기존 셀프서비스 진입 인사말(Story 1.2) 흐름 뒤에 자연스럽게 이어지고 응답 지연을 유의미하게 늘리지 않는다.

## Tasks / Subtasks

- [x] **Task 1: 도메인별 "미완료" 판정 규칙 정의** (AC: 1) — ✅ 완료 (2026-07-15)
  - [x] 매뉴얼 §2 "초기 설정 체크리스트"의 **필수** 항목만 다룸(예약 도메인은 카탈로그에 없어 제외, chat-relay/integrations는 매뉴얼에 "필요하다면/받는다면"으로 명시된 선택 항목이라 제외):
    - `persona`: `exists=False` 또는 `name`/`description` 모두 빈값이면 미완료
    - `ai-escalation`: persona 자체가 없으면(`persona_exists=False`) "AI 에스컨레이션 방식 미결정"으로 간주(escalation_mode="hitl" 기본값은 관리자가 의식적으로 선택한 것이 아니기 때문)
    - `call-control`: `rules`가 빈 리스트면 미완료
  - [x] 판정 규칙을 `self_service/onboarding.py`(신규)에 배치 — 카탈로그(`settings_catalog.py`)는 순수 조회만 유지(Story 1.4 IV1과 충돌 방지)
  - [x] 부가: `settings_catalog._get_ai_escalation()`에 `persona_exists` 필드를 추가해 ai-escalation 판정이 persona 조회를 중복 호출하지 않고도 구별가능하도록 개선

- [x] **Task 2: `get_onboarding_checklist` Tool 구현** (AC: 1) — ✅ 완료 (2026-07-15)
  [Source: [architecture#new-components](../architecture/self-service-ai-assistant-architecture.md) — `self_service/tools.py`]
  - [x] `self_service/onboarding.py::get_onboarding_checklist(owner)`가 `settings_catalog.list_domains()`가 아닌 명시적 `_CHECKS` 목록(도메인, 판정 함수, 안내 문구)을 순회하여 미완료 항목 목록 생성(모든 7개 도메인을 순회하지 않고 필수 3개만 조회 — IV2 지연 최소화)
  - [x] `self_service/tools.py`(신규) — `booking_tools.py::_make_tool` 패턴을 그대로 재현(private 심볼 직접 import 대신 동일 패턴을 복제)해 `get_onboarding_checklist_tool` LangGraph Tool 등록(JSON 문자열 반환, 에러 흡수)

- [x] **Task 3: 셀프서비스 진입 인사말에 체크리스트 통합** (AC: 2, 3) — ✅ 완료 (2026-07-15)
  [Source: Story 1.2 — `self_service_agent_node`]
  - [x] `self_service_agent_node`가 세션 첫 턴(`state["messages"]`가 비어있을 때)에만 `get_onboarding_checklist(owner)`를 호출해 시스템 프롬프트 `[온보딩 체크리스트]` 섹션에 주입
  - [x] 미완료 항목이 있으면 인사 뒤 자연스럽게 안내하고 설정 도움을 제안하도록 LLM 프롬프트 규칙에 명시(하드코딩 라우팅 없이 프롬프트 지시로만 처리 — 실제 자동설정 Tool 연결은 Story 1.8에서 구현)
  - [x] 미완료 항목이 없으면(AC3) 또는 첫 턴이 아니면 체크리스트를 전혀 언급하지 않으도록 플레이스홀더 분기 처리

## Dev Notes

### Previous Story Insights

Story 1.4에서 `settings_catalog.list_domains()`/`get_domain_value()`가 이미 구현되어 있다. 본 Story는 이를 소비하는 첫 번째 기능이다. Story 1.2의 `self_service_agent_node` 골격에 온보딩 체크리스트 호출을 추가한다.

### File Locations

```
src/ai_voicebot/self_service/onboarding.py                          # 신규(구현 완료) — 미완료 판정 로직
src/ai_voicebot/self_service/tools.py                                # 신규(구현 완료) — get_onboarding_checklist_tool
src/ai_voicebot/langgraph/nodes/self_service_agent.py                # 기존 파일(Story 1.2/1.3) 수정 완료
tests_new/unit/test_ai_voicebot/test_self_service_onboarding.py      # 신규(구현 완료, 프로젝트 실제 컨벤션 경로)
```

### Technical Constraints

- **IV1**: 완료 여부를 캐싱하거나 별도 DB에 저장하지 않는다 — 매번 카탈로그를 통해 실시간 조회(단일 진실 소스).
- **IV2**: 체크리스트 조회가 7개 도메인 전체를 매번 순회하므로, 도메인 수가 늘어나도 지연이 커지지 않도록 병렬 조회(`asyncio.gather` 등)를 고려— 단, 각 `get_fn`이 동기 함수라면 순차 호출도 무방(실제 지연 측정 후 결정).

## Testing

### Testing Standards

- **Test file location**: `tests_new/unit/test_ai_voicebot/test_self_service_onboarding.py`(프로젝트 실제 컨벤션인 `tests_new/unit/test_<area>/test_<name>.py` 패키지 구조 적용, Story 1.1~1.4와 동일 위치)
- **Framework**: pytest
- **필수 테스트 케이스**:
  1. 미완료 항목이 있는 테넌트 → 체크리스트에 해당 항목 포함
  2. 모든 항목 완료된 테넌트 → 빈 체크리스트 반환(AC3)
  3. 체크리스트 조회가 카탈로그 조회 함수만 호출하고 별도 저장소를 만들지 않음(IV1, 코드 리뷰 기준)
  4. 체크리스트 안내 포함 세션의 첫 응답 지연이 NFR1 기준 내(IV2)

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                             | Author                  |
| ---------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 2026-07-14 | 0.1     | 최초 Draft 생성(시퀀싱 정정 후 Story 1.4→1.5로 재배치)                                                                                                                                                                                                                                  | Copilot (BMAD SM 역할)  |
| 2026-07-15 | 0.2     | Task 1~3 구현 완료 — `onboarding.py`(필수 3개 도메인 미완료 판정), `tools.py`(get_onboarding_checklist_tool), `self_service_agent_node`에 세션 첫 턴 체크리스트 주입 통합. `settings_catalog.py`에 `persona_exists` 필드 추가(부수적 개선). 신규 테스트 18건 전체 통과. Status → Review | Copilot (BMAD Dev 역할) |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 4.5 (GitHub Copilot)

### Debug Log References

- `python -m pytest tests_new/unit/test_ai_voicebot/test_self_service_onboarding.py -v --no-cov` → 18 passed
- `python -m pytest tests_new/unit/test_ai_voicebot tests_new/unit/test_events -q --no-cov` → 92 passed, 회귀 없음

### Completion Notes List

- Task 1: 필수 항목을 3개(persona/ai-escalation/call-control)로 제한하고, 예약(카탈로그에 없음)와 chat-relay/integrations(매뉴얼에 명시적 선택 항목)는 의도적으로 제외함을 모듈 docstring에 명시.
- Task 1: `ai-escalation` 판정은 자체 필드만으로는 "기본값 hitl"과 "의식적 선택한 hitl"을 구별할 수 없음을 발견 → `settings_catalog._get_ai_escalation()`에 `persona_exists` 플래그를 추가해 해결(Story 1.4 파일 최소 수정, 기존 테스트 회귀 없음 확인).
- Task 2: `_make_tool` 헬퍼는 `booking_tools.py`의 private 심볼을 직접 import하지 않고 동일 패턴을 `self_service/tools.py`에 복제(모듈 간 private 심볼 결합 회피).
- Task 3: `is_first_turn = not state.get("messages")`로 세션 첫 턴을 판별 — 첫 턴에만 체크리스트를 조회해 불필요한 반복 조회(지연 증가)를 피함(IV2).
- Task 3: 체크리스트 조회 예외는 노드 응답 생성을 막지 않도록 흡수(온보딩 실패 ≠ 전체 응답 실패).

### File List

- `src/ai_voicebot/self_service/onboarding.py` (신규)
- `src/ai_voicebot/self_service/tools.py` (신규)
- `src/ai_voicebot/self_service/settings_catalog.py` (수정 — `persona_exists` 필드 추가)
- `src/ai_voicebot/langgraph/nodes/self_service_agent.py` (수정 — 온보딩 체크리스트 통합)
- `tests_new/unit/test_ai_voicebot/test_self_service_onboarding.py` (신규, 18 tests)

## QA Results

**검토일**: 2026-07-15 | **검토자**: Copilot (BMAD PO/QA 역할 겸임, Epic 레벨 통합 검토)

AC/IV 전체 코드·테스트 대조 완료. 상세 근거는 Epic 레벨 통합 리포트 참고:
[2026-07-15_self_service_epic1_po_qa_review.md](../reports/2026-07/2026-07-15_self_service_epic1_po_qa_review.md)

- ✅ 승인(PASS) — 회귀 테스트 포함 전체 통과, 추가 조치 없음
