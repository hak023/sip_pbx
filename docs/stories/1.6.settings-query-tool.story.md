# Story 1.6: 설정 조회 Tool (읽기 전용, 전체 도메인)

## Status

Draft

## Story

**As a** 테넌트 관리자,
**I want** "지금 알림 설정이 어떻게 되어 있어?"처럼 물어보면 현재 값을 답변받기를,
**so that** 대시보드에 접속하지 않고도 현재 상태를 확인할 수 있다.

## Acceptance Criteria

1. `get_self_service_settings(domain)` Tool이 **설정 카탈로그(Story 1.4)에 등록된 모든 도메인**의 현재 값을 조회해 구조화된 값을 반환한다(카탈로그 조회 함수를 LangGraph Tool로 노출하는 얇은 래퍼).
2. 카탈로그에 등록되지 않은 도메인/필드 조회 요청 시 "확인해드릴 수 없는 항목"으로 안내한다.
3. 조회 결과는 실제 설정값과 100% 일치한다(프론트엔드와 동일 데이터 소스 사용, FR10).

### Integration Verification

- **IV1**: 이 Tool이 Story 1.4의 카탈로그 조회 함수만 호출하며 별도 조회 로직을 중복 구현하지 않는다.

## Tasks / Subtasks

- [ ] **Task 1: `get_self_service_settings` Tool 구현** (AC: 1, 2)
  [Source: [architecture#new-components](../architecture/self-service-ai-assistant-architecture.md) — `self_service/tools.py`]
  - [ ] `settings_catalog.get_domain_value(domain, owner)`를 감싸는 얇은 Tool 함수 작성(로직 없이 위임만 — IV1)
  - [ ] `domain` 파라미터가 카탈로그에 없으면 사용자 친화적 오류 메시지 반환(AC2)
  - [ ] `booking_tools.py::_make_tool` 패턴으로 LangGraph Tool 등록

- [ ] **Task 2: `self_service_agent_node`에 Tool bind** (AC: 1)
  - [ ] Story 1.2/1.5에서 이미 구성된 LLM+Tool 루프에 `get_self_service_settings` 추가
  - [ ] 시스템 프롬프트에 "설정 조회 시 반드시 이 Tool을 호출하여 최신 값을 확인하라" 지시 추가(RAG 환각 방지, PRD Brief §Risks 반영)

- [ ] **Task 3: 데이터 일치성 테스트** (AC: 3)
  - [ ] 프론트엔드가 실제로 표시하는 값과 Tool 조회 결과를 비교하는 테스트(같은 서비스 함수를 호출하므로 자동으로 일치해야 함 — 회귀 방지용 계약 테스트)

## Dev Notes

### Previous Story Insights

Story 1.4의 `settings_catalog.get_domain_value()`가 이미 owner 파라미터 기반으로 동작한다. 본 Story는 그것을 LangGraph Tool 형태로 노출하는 것 외에 새로운 로직이 거의 없다 — **가장 단순한 Story**.

### File Locations

```
src/ai_voicebot/self_service/tools.py   # 기존 파일(Story 1.5) 수정 — get_self_service_settings 추가
```

### Technical Constraints

- **FR10**: 별도 캐시를 두지 않는다 — 매 호출 시 카탈로그를 통해 실시간 조회.

## Testing

### Testing Standards

- **Test file location**: `tests_new/test_self_service_tools.py`(Story 1.5 테스트 파일과 통합 가능)
- **Framework**: pytest
- **필수 테스트 케이스**:
  1. 등록된 도메인 조회 시 정상 값 반환
  2. 미등록 도메인 조회 시 사용자 친화적 오류 메시지(AC2)
  3. Tool이 `settings_catalog.get_domain_value()` 외 다른 조회 경로를 사용하지 않음(코드 리뷰 기준, IV1)

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-07-14 | 0.1 | 최초 Draft 생성 | Copilot (BMAD SM 역할) |

## Dev Agent Record

### Agent Model Used

_(구현 시작 시 기록)_

### Debug Log References

_(구현 시작 시 기록)_

### Completion Notes List

_(구현 시작 시 기록)_

### File List

_(구현 시작 시 기록)_

## QA Results

_(QA 에이전트 리뷰 전)_
