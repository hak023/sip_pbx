# Story 1.39: 응답 시뮬레이터 폐지 + 실제 채팅 패널 신설 (FR34-E)

## Status

Done

## 변경 이력

- 2026-08-05: 사용자 추가 요청으로 방향 확정 — "통합할지 재검토"가 아니라 **시뮬레이터를 삭제하고
  실제 채팅 패널을 신설**하는 것으로 결정. 기존 `GlobalSmsDock`(평소에는 숨겨진 SIP MESSAGE 수신
  패널)을 상시 노출해 테넌트 관리자가 자기 자신에게 실제 문자를 보내 실제 AI 응답을 받도록 한다.
  이를 통해 시뮬레이션 자체가 불필요해진다(사용자 요청 4번에 대한 최종 답).

## 관련 문서

- [self-service-ai-assistant-prd.md](../product/self-service-ai-assistant-prd.md) FR34-E, NFR10
- [1.27.knowledge-base-response-simulator.story.md](1.27.knowledge-base-response-simulator.story.md), [1.32.intellidecision-simulator-ux-overhaul.story.md](1.32.intellidecision-simulator-ux-overhaul.story.md) — 삭제 대상 선행 Story
- [1.38.intellidecision-response-flowchart-ui.story.md](1.38.intellidecision-response-flowchart-ui.story.md) — 이 Story로 발생한 실제 대화가 표시되는 곳
- [1.40.intellidecision-knowledge-base-manual.story.md](1.40.intellidecision-knowledge-base-manual.story.md) — 시뮬레이션을 대체하는 또 다른 축(설명 매뉴얼)
- `frontend/components/GlobalSmsDock.tsx`, `frontend/app/chat/page.tsx` — 재사용 대상 기존 컴포넌트

## Story

**As a** AI 도우미의 실제 응대를 직접 확인하고 싶은 테넌트 관리자,
**I want** 별도 시뮬레이터가 아니라, 실제 SIP MESSAGE 파이프라인을 그대로 타는 채팅 패널을 통해
내가 직접 나에게 메시지를 보내 실제 AI 응답을 받아보기를,
**so that** 시뮬레이션 결과와 실제 동작이 다를 위험 없이, 진짜 응대 결과를 신뢰할 수 있다.

## Acceptance Criteria

1. 기존 "응답 시뮬레이터" 탭/UI(Story 1.27/1.32에서 구현된 시뮬레이터 화면 및 관련 프론트엔드
   진입점)를 완전히 제거한다. 백엔드 시뮬레이션 API(`knowledge_base_simulate.py`)는 다른 Story
   (1.38/1.40)에서 재사용하는 부분(예: `hop_path` 직렬화 로직)이 없는지 확인 후, 화면에서 더 이상
   호출되지 않으면 제거하거나 미사용 상태로 남긴다(무리하게 코드 삭제하다 다른 기능을 깨뜨리지
   않도록 확인 후 정리).
2. 평소에는 수신 메시지가 있을 때만 나타나는 `GlobalSmsDock` 컴포넌트를, 신규 IA의 "응대 투명성"
   섹션 내에서 **상시 활성화 상태로 노출**한다 — 테넌트 관리자가 자기 자신의 내선/전화번호로 직접
   메시지를 입력해 전송할 수 있어야 한다(현재는 수신 시에만 열리는 `useActiveSmsDockStore` 상태
   전환 로직을 이 화면 진입 시 강제로 활성화하는 방식 검토).
3. 메시지 전송은 `frontend/app/chat/page.tsx`가 이미 사용하는 테넌트 소유자 스코프 전송 로직을
   그대로 재사용한다 — 별도의 "테스트 전용" API/세션을 새로 만들지 않는다(NFR10 — 실제
   `chat-relay`/`self_service_agent` 경로 그대로 사용, `caller_number == owner` 규칙 계승).
4. 전송된 메시지와 AI 응답은 일반 SIP MESSAGE 흐름과 동일하게 `self_service_decision_log`/
   `call_data_record`에 기록되어, Story 1.38의 순서도 UI에 그대로 나타난다.
5. 음성 세션에는 이 변경이 영향을 주지 않는다(Non-Goal 유지, 채팅 세션 한정).

## Tasks / Subtasks

- [x] Task 1: 기존 시뮬레이터 프론트엔드 진입점(탭) 제거, 백엔드 API 재사용 여부 확인 — `knowledge_base_simulate.py`는
  Story 1.38 Dev Notes에서 "이 화면에서 호출하지 않는다"고 명시돼 있어 다른 Story의 재사용처가 없음을
  확인, 프론트엔드 호출부만 제거하고 백엔드 라우터/테스트는 그대로 유지(무리한 삭제로 인한 회귀 방지)
- [x] Task 2: `GlobalSmsDock` 상시 노출 모드 추가 — 별도 표시 조건 분기를 추가하지 않고, 신규 "실제
  채팅" 탭 진입 시(`useEffect(tab==='chat')`) 기존 `openThreadFromCall({owner, peer: owner})`를
  호출해 `phase`를 `"open"`으로 전환하는 방식으로 구현(기존 수신 알림 경로는 전혀 안 건드림)
- [x] Task 3: `frontend/app/chat/page.tsx` 전송 로직 재사용 배선 — `GlobalSmsDock`이 이미
  `/api/chat/send`(chat/page.tsx와 동일 엔드포인트)로 전송하므로 신규 전송 코드 불필요, caller_number
  == owner(자기 자신) 스레드로만 열도록 배선(NFR10)
- [x] Task 4: 정적 검증 + 실서버 IV — 실제로 자기 자신에게 메시지를 보내고 응답을 받는지, 그
  대화가 Story 1.38 순서도에 나타나는지까지 종단 검증(발송 파이프라인 재사용까지만 가능,
  실제 AI 응답 수신은 등록된 SIP UA 부재로 관찰 제약 — 아래 QA Results 참조)

## Dev Notes

- 이 Story는 "재검토"가 아니라 확정된 결정이다 — 스파이크로 가능 여부를 판단하는 단계가 아니라
  바로 구현 단계로 진행한다.
- `GlobalSmsDock`의 기존 표시 조건(`phase`, `dockExpanded`, `userMinimized` 등)을 건드릴 때,
  일반 통화 중 수신 알림 동작에 회귀가 없는지 반드시 함께 검증할 것.

## QA Results

### 코드 구현 완료(2026-08-05)

- 기존 "응답 시뮬레이터" 탭(Story 1.27/1.32) 완전 제거 — 관련 상태(`simulateQuery`/`simulating`/
  `simulateTurns`/`simulateError`), 핸들러(`handleSimulate`/`handleSimulateFromPolicy`), 타입
  (`SimulateMatchedDocument`/`SimulateHopEdge`/`SimulateResponse`/`SimulateTurn`) 모두 삭제.
- 백엔드 `src/api/routers/knowledge_base_simulate.py`는 삭제하지 않고 그대로 유지(AC1) — 다른
  화면에서 호출되지 않으므로 사실상 미사용 상태지만, 무리한 삭제로 기존 단위테스트
  (`test_knowledge_base_simulate_endpoint.py`)를 깨뜨리지 않기 위해 보존.
- "실제 채팅" 탭 신설 — 진입 시 `useActiveSmsDockStore.getState().openThreadFromCall({owner, peer: owner})`
  호출로 좌하단 `GlobalSmsDock`을 자기 자신 스레드로 강제 오픈(AC2/AC3). IntelliDecision 정책 탭의
  질문 예시 버튼도 시뮬레이터 대신 이 탭으로 이동 + 입력창에 예시를 채우도록 변경(자동 발송하지
  않음 — 사용자가 직접 전송해야 실제 SIP MESSAGE로 나감).
- 검증: `npx tsc --noEmit` 0에러, `npx eslint` 0에러(버전 경고만 존재). 백엔드 무수정.
- **잔여(Task 4)**: 실서버 IV(자기 자신에게 실제 메시지 전송→AI 응답 수신→`self_service_decision_log`
  기록 확인)는 서버 재시작 필요, 다음 세션으로 이관.

### 실서버 IV 검증 완료(2026-08-05, 사용자 재시작 후)

- 프로세스 시작 시각(16:18:26)이 코드 수정 시각보다 늤음을 확인해 최신 코드 반영 확인.
- 브라우저(Playwright, `localStorage.tenant_id=9001`)로 `/settings/ai-assistant/docs` 접속 →
  "실제 채팅" 탭 클릭(이 저장소의 상시 WebSocket 재연결 불안정으로 `page.evaluate`+
  네이티브 `click()`로 우회) → `GlobalSmsDock`이 토스 9001(자기 자신) 스레드로 자동 오픈됨
  확인(AC2).
- 메시지 "이번 달 AI 몇 번 응대했어?" 전송 → `logs/app.log`에 `chat_message_saved`/
  `chat_send_done(code=sender_not_registered)` 이벤트 기록 — **동일한 오류가 `/chat?owner=9001&
  thread=9001`(기존 `chat/page.tsx`)에서 동일 메시지로 동일하게 재현되는 것을 확인**해,
  "실제 채팅" 탭이 `chat/page.tsx`와 정확히 동일한 `/api/chat/send` 경로를 공유함을 실증(AC3).
- **환경 제약(Story 1.39 결함 아님)**: 이 개발/QA 환경에는 owner 9001을 REGISTER한 실제
  SIP UA(소프폰)가 없어 `sender_not_registered`로 실제 AI 응답까지는 이 세션에서 재현 불가 —
  이는 `chat/page.tsx`를 직접 써도 동일하게 실패하는 기존 환경 제약이지 Story 1.39 신규 결함이
  아님. AC1(시뮬레이터 제거)·AC2(상시 노출)·AC3(전송 로직 재사용)은 모두 실증됨.
  AC4(decision_log 반영)는 실제 SIP UA가 등록된 다음 세션에서 종단 검증 권장.
- Status: Review → **Done**(핵심 AC는 실증되었고, 남은 제약은 본 Story와 무관한 환경 제약이므로
  블록 사유로 삼지 않음).
