# Story 1.44: 응대 유형 탐색기(IntelliDecision × 실제 채팅 통합)

## Status
Review

> 상세 배경·UI 미리보기(검토 필요): [Epic 4 계획 리포트](../reports/2026-08/2026-08-06_epic4_platform_maturation_and_ux_transparency_planning.md) §5(FR35-D), 와이어프레임은 리포트 본문 참고.

## Story
As a 테넌트 관리자,
I want 유형 A~I별 질문 예시를 골라 어떤 지식·hop이 활용될지 먼저 투명하게 확인한 뒤, 바로 이어서
실제 AI와 대화할 수 있기를,
so that AI 응대 방식을 신뢰하고, 필요하면 데이터를 조정해 응대를 바꿀 수 있다는 통제감을 가진다.

## Acceptance Criteria
1. 유형 A~I 9개 탭이 있고, 각 탭에는 해당 유형의 `trigger_examples` 목록이 선택 가능한
   리스트로 표시된다.
2. 각 질문 예시 옆에 `rag_enabled`/`requires_tool`/`rag_source_scope`(하이브리드 여부 포함)
   기반 배지가 표시된다.
3. 질문을 선택하면 `knowledge_base_simulate.py`(Story 1.32)를 재사용해 실제 벡터 검색을
   실행하고, 매칭 문서·hop 경로를 사람이 읽는 카드 UI로 보여준다. **LLM 응답 텍스트는 생성하지
   않는다**(Story 1.39/1.40 "시뮬레이션 금지" 원칙 계승, 카드에도 "이건 예측이며 실제 답변은
   실제 대화에서 확인하라"는 문구 명시).
4. "이대로 실제로 물어보기" 버튼을 누르면 `useActiveSmsDockStore.getState().openThreadFromCall()`
   (Story 1.39 패턴)로 실제 채팅 패널이 해당 질문과 함께 열린다.
5. 실제 채팅 패널에서는 기존과 동일하게 추가 질문을 계속 입력할 수 있다(신규 멀티턴 상태 관리
   코드 불필요 — Story 1.39 컴포넌트 재사용).
6. 백엔드 신규 API는 최소화한다 — 기존 `/intellidecision-policy`, `/knowledge-base/simulate`
   엔드포인트에 필드 추가가 필요한 경우만 신규 파라미터를 추가한다(신규 엔드포인트 추가는
   최후 수단).

## Tasks / Subtasks
- [x] Task 1: 기존 `/intellidecision-policy`/`/intellidecision-manual` 응답으로 AC 충족 가능한지 재확인
      — 유형 탭/질문리스트/배지는 이미 구현되어 있었으나(Story 1.32/1.40), 선택한 특정 질문에
      대한 매칭 미리보기는 trigger_examples[0]에만 고정되어 있어(Story 1.40) 신규 엔드포인트 필요로 판단
- [x] Task 2: 백엔드 — `intellidecision_manual.py::build_case_example_for_query(spec, owner, query)`
      신규(기존 `build_case_example_for_type`는 이를 `trigger_examples[0]`로 호출하도록 리팩토링) +
      `GET /api/settings/ai-assistant/intellidecision-manual/preview?owner=&code=&query=` 신규(LLM 호출
      없음, `knowledge_base_simulate.py`의 `process_utterance()` 방식은 의도적으로 재사용하지 않음)
- [x] Task 3: 프론트엔드 `IntentExplorer` 확장(신규 컴포넌트 대신 기존 "AI 의사결정 로직" 탭에 통합 — 이미
      유형 A~I 탭/질문리스트/배지가 있어 별도 컴포넌트 생성은 중복이라고 판단): 질문 버튼 클릭 시
      `handleExplorePreview()`가 위 API를 호출해 매칭 문서/hop 개수 + "이건 예측" 문구를 보여주고, "이대로
      실제로 물어보기" 버튼이 눈러야 기존 `handleChatFromPolicy()`(Story 1.39 실제 채팅 패널 연결)로 이어진다
- [x] Task 4: 단위테스트(`test_intellidecision_manual.py`에 `TestBuildCaseExampleForQuery` 4건 추가) +
      `tsc --noEmit`/`eslint` 검증
- [ ] Task 5: 실서버 IV(실제 업로드 문서 기반 질문 선택 → 미리보기 응답 확인 → "실제로 물어보기" 클릭 → 실제
      채팅 패널 이동 확인)

## Dev Notes
- 이 화면은 "응답 시뮬레이터"의 재도입이 아니다 — 실시간 LLM 호출이 전혀 없다는 점을 코드
  리뷰·QA에서 반드시 재확인할 것(Story 1.39/1.40이 이미 제거한 것을 실수로 되살리지 않도록).
- 유형 C는 Story 1.45(하이브리드 표시 보강)와 함께 검토.

## Dev Agent Record
- **Task 1 재확인으로 설계 변경(중요)**: 리포트/PRD 초안은 `knowledge_base_simulate.py`
  (Story 1.27/1.32) 재사용을 제안했으나, 이 엔드포인트는 실제로 `ConversationAgent.
  process_utterance()`를 호출해 **진짜 LLM 응답을 생성**함을 코드로 확인함 — 이를 그대로 쓰면
  Story 1.39/1.40이 명시적으로 폐기한 "응답 시뮬레이터"를 그대로 재도입하는 꼴이 된다. 대신
  Story 1.40의 안전한 패턴(`intellidecision_manual.py`, 벡터 검색+그래프 조회만, LLM 미호출)을
  임의 질문으로 확장해 재사용했다.
- `build_case_example_for_type()`는 이제 `build_case_example_for_query(spec, owner,
  spec.trigger_examples[0])`를 호출하도록 리팩토링 — 기존 호출부/테스트 11건 무수정으로 통과.
- 프론트엔드는 신규 컴포넌트를 만들지 않고 기존 "AI 의사결정 로직" 탭(Story 1.32)의 질문 버튼
  클릭 동작만 교체 — 기존 유형 탭/배지/hop 시각화 UI는 그대로 재사용해 중복 구현을 피함.
- 신규 단위테스트 4건(`TestBuildCaseExampleForQuery`) + 기존 self_service/intellidecision 관련
  회귀 328건 전체 통과, `tsc --noEmit`/`eslint` 0에러, 라우터 임포트로 신규 엔드포인트
  등록 확인(`/api/settings/ai-assistant/intellidecision-manual/preview`).
- **잔여**: 실서버 IV(Task 5) — 서버 재시작 필요, 사용자 승인 대기.

## QA Results
(대기)

*최종 업데이트: 2026-08-06*
