# 🔍 로그 분석 결과

**날짜**: 2026-03-09 15:03
**Call ID**: `UztQDCBEBq`

---

## 문제 1: 409 STT Stream Timeout 🔴 CRITICAL

### 에러 로그
```
15:03:01.068 | ERROR | GoogleSTTService#0 exception: 
Unknown error occurred: 409 Stream timed out after receiving no more client requests.
```

### 원인
**Google Service Singleton이 적용되지 않음!**

- **서버 로그 확인**:
  ```
  14:35:37.397 | Pipecat 0.0.102 (서버 1)
  15:00:33.550 | Pipecat 0.0.102 (서버 2)
  ```
  
- **문제**: `get_or_create_google_stt_service()` 호출이 `main.py`에 추가되었지만, **실제로 실행되지 않음**
- **증거**: 로그에 `🔥 [AI Background] Google STT/TTS Service 사전 초기화 중...` 메시지가 **없음**

### 결과
- 통화마다 새로운 `GoogleSTTService` 생성
- STT 스트림이 초기화되지 않은 상태에서 오디오 프레임 전달
- 60초 후 Google API 타임아웃 (409 에러)

---

## 문제 2: RTP 패킷 유실 🟠 HIGH

### 통계 (Call ID: UztQDCBEBq)

| 항목 | 값 |
|------|-----|
| 총 전송 패킷 | 1213+ |
| 간격 이탈 횟수 | **650+** |
| 이탈률 | **54%** |
| 기대 간격 | 20ms |
| 실제 간격 | 14~31ms (매우 불규칙) |

### 로그 증거
```
15:03:15.983 - rtp_interval_violation: actual_ms=14.7, packets_sent=564, violation_count=300
15:03:17.878 - rtp_interval_violation: actual_ms=14.5, packets_sent=643, violation_count=350
15:03:20.058 - rtp_interval_violation: actual_ms=26.0, packets_sent=732, violation_count=400
15:03:22.088 - rtp_interval_violation: actual_ms=26.9, packets_sent=819, violation_count=450
15:03:24.440 - rtp_interval_violation: actual_ms=30.9, packets_sent=917, violation_count=500
15:03:26.316 - rtp_interval_violation: actual_ms=26.3, packets_sent=995, violation_count=550
15:03:32.842 - rtp_interval_violation: actual_ms=31.3, packets_sent=1102, violation_count=600
15:03:35.398 - rtp_interval_violation: actual_ms=26.2, packets_sent=1213, violation_count=650
```

### 원인 분석

#### 1. 큐 과적재
```
queue_size: 25 → 44 → 69 → 124 → 149 → 342 → 367 → 388
```
- TTS가 생성하는 속도 > RTP 발송 속도
- 큐가 400개 가까이 쌓임
- Burst 전송으로 인한 간격 붕괴

#### 2. 정밀 타이머 미작동
- `time.perf_counter()` 기반 타이머를 추가했지만
- **여전히 14~31ms로 불규칙**
- 가능한 원인:
  - `asyncio.sleep()` 자체의 부정확성 (Windows)
  - 다른 Task의 CPU 점유

---

## 문제 3: STT 작동 여부 ✅ **실제로 작동 중!**

### 증거

#### ✅ STT가 **정상 작동함**
```
15:03:20.562 | rag_llm_user_input: text="Beautiful."
15:03:20.562 | timing_stt_final_to_rag: text_preview="Beautiful."
15:03:27.232 | llm_response_sent: user_text="Beautiful.", response="감사합니다. 더 궁금하신 점 있으시면 편하게 말씀해 주세요."
```

**사용자가 "Beautiful"이라고 말했고, LLM이 응답함!**

#### ✅ Input 프레임 정상 전달
```
15:03:15.362 - input_audio_frame_to_pipeline: frame_count=1600
15:03:17.366 - input_audio_frame_to_pipeline: frame_count=1700
15:03:19.148 - input_audio_frame_to_pipeline: frame_count=1800
15:03:21.406 - input_audio_frame_to_pipeline: frame_count=1900
...
15:04:08.106 - input_audio_frame_to_pipeline: frame_count=4100 (통화 종료 직전)
```

**4100개 프레임 = 82초 분량 = 통화 전체 기간**

### 실시간 STT 로그 관련

#### 현재 상태
- **실시간 STT는 로그로 남지 않음** (interim 결과)
- **최종 STT만 로그에 나타남** (`rag_llm_user_input`)

#### 이유
`ai_orchestrator.py`에서 실시간 STT를 WebSocket으로만 전송:

```python
async def _on_stt_result(self, text: str, is_final: bool):
    # WebSocket으로만 전송 (로그 없음)
    if websocket_manager and self.call_id and text:
        await websocket_manager.emit_stt_transcript(...)
```

### 개선 방법: 실시간 STT 로깅 추가

```python
async def _on_stt_result(self, text: str, is_final: bool):
    # 로그 추가
    logger.info("stt_result",
               call_id=self.call_id,
               text=text,
               is_final=is_final,
               category="stt",
               progress="stt")
    
    # WebSocket 전송
    if websocket_manager and self.call_id and text:
        await websocket_manager.emit_stt_transcript(...)
```

---

## 해결 방안

### 1. Google Service Singleton 실제 적용 🔴 필수

#### 문제
`main.py`에 코드는 추가했지만 **실행되지 않음**

#### 원인
코드 배치 위치가 잘못됨 (확인 필요)

#### 해결
`main.py` 점검 및 실제 실행 확인

---

### 2. RTP 패킷 유실 개선 🟠 중요

#### 방법 1: 큐 크기 더 줄이기
```python
# Before
self._pipecat_outgoing_queue = asyncio.Queue(maxsize=500)

# After
self._pipecat_outgoing_queue = asyncio.Queue(maxsize=100)  # 더 작게
```

#### 방법 2: TTS 속도 제한
```python
# send_audio_to_caller에서
if self._pipecat_outgoing_queue.qsize() > 50:
    await asyncio.sleep(0.01)  # 큐가 많으면 대기
```

#### 방법 3: Windows용 정밀 타이머
```python
import time

# asyncio.sleep 대신 busy-wait (정밀도 향상)
target_time = time.perf_counter() + interval_sec
while time.perf_counter() < target_time:
    pass  # Busy wait
```

---

### 3. 실시간 STT 로그 추가 ✅ 선택

`ai_orchestrator.py:_on_stt_result`에 로깅 추가

---

## 요약

| 문제 | 상태 | 원인 | 해결 |
|------|------|------|------|
| **409 STT Timeout** | 🔴 Critical | Singleton 미적용 | `main.py` 수정 필요 |
| **RTP 패킷 유실** | 🟠 High | 큐 과적재 + 타이머 | 큐 100으로 감소 |
| **STT 작동** | ✅ **정상** | 로그만 없음 | 로깅 추가 |

---

## 다음 단계

1. **`main.py` 점검**: Singleton 코드가 실행되는지 확인
2. **큐 크기 감소**: 500 → 100
3. **실시간 STT 로깅 추가**: `_on_stt_result`
4. **재테스트**
