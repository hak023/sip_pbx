# 통화 리뷰 리포트 — call_id: ZNh~RK-IOg

- 작성일: 2026-04-10 16:00
- 대상 통화: `ZNh~RK-IOg`
- 통화 일시: 2026-03-21 20:06:39 ~ 20:11:03 (총 약 4분 24초)
- 수신 번호(callee): `1004` (기상청 AI 통화 비서)
- 로그 소스: `logs/call_data_record_20260321.log`

---

## 1. 통화 타임라인 요약

| 순번 | 시각 | 사용자 발화 / 이벤트 | AI 응답 | LLM 지연 | 신뢰도 | 비고 |
|---|---|---|---|---|---|---|
| - | 20:06:39 | *(통화 연결)* | — | — | — | — |
| - | 20:06:40 | — | 인사 1단계 안내 | — | — | |
| - | 20:06:45 | — | 인사 2단계 안내 (기능 소개) | — | — | |
| 1 | 20:07:02 | "내일의 날씨에 대해서 알려주세요." | "실시간 정보는 홈페이지 확인… 담당자 연결할까요?" | **20.1s** | 0.27 | ⚠️ 지연 심각, 신뢰도 낮음, HITL 요청 |
| 2 | 20:07:44 | "네 연결해 주세요." | *(응답 없음)* | — | — | ❌ 담당자 연결 실패 |
| 3 | 20:08:10 | "찾아가는 길에 대해서 궁금합니다 알려주세요." | "택시를 이용하시면…" | **10.6s** | 0.29 | ⚠️ 지연, 신뢰도 낮음, HITL 요청 |
| 4 | 20:08:48 | "혹시 어떤 일을 할 수 있나요?" | "날씨 예보 조회, 기상 특보 안내…" | **2.5s** | 0.90 | ✅ 정상 |
| 5 | 20:09:08 | "일반 기상 상식을 안내해 주세요." | "해당 내용은 확인이 필요합니다. 잠시만 기다려 주세요." | **32.0s** | 0.34 | ❌ 지연 최악, 폴백 응답, HITL 요청 |
| - | 20:10:02 | — | *(운영자 HITL 응답: "내일은 비가 올 예정입니다.")* | — | — | HITL 응답 수신 |
| 6 | 20:10:24 | "네 감사합니다." | "기상 정보 이용에 도움이 되었기를 바랍니다." | **37.8s** | 0.00 | ❌ 통화 종료 후 처리, HITL 오발동 |
| - | 20:10:36 | *(통화 종료)* | — | — | — | — |

---

## 2. 발견된 문제점

---

### 🔴 P1 — LLM 응답 지연 심각 (3회 발생)

| 발화 | 지연 |
|---|---|
| "내일의 날씨에 대해서 알려주세요." | **20.1초** |
| "일반 기상 상식을 안내해 주세요." | **32.0초** |
| "네 감사합니다." | **37.8초** (통화 종료 후 처리) |

**원인 분석:**
- 3건 모두 RAG 검색 결과의 유사도가 매우 낮아 (`soft_fallback_applied: true`, 최고 score ≤ 0.34) LLM이 문맥 없이 응답을 생성해야 하는 상황.
- 특히 발화 5 ("일반 기상 상식")와 발화 6 ("감사합니다")은 LLM 처리 중 통화가 종료됐음에도 파이프라인이 계속 실행됨.
- `agent_elapsed`가 20~38초 수준은 LLM API 호출 자체의 타임아웃/응답 지연 또는 LangGraph 노드 과부하로 추정.

**영향:** 고객이 침묵 구간(20~38초)을 경험하게 되어 통화 품질에 직접적으로 악영향.

---

### 🔴 P2 — 담당자 연결 실패 (`transfer_contact_not_found`)

**이벤트:** 20:07:45 `transfer_contact_not_found`

**상황:**
- AI가 첫 번째 응답에서 "담당자에게 연결해 드릴까요?"를 제안함.
- 고객이 "네 연결해 주세요."라고 명확히 답함.
- 시스템이 `transfer_request_detected` 후 즉시 `transfer_contact_not_found` 처리.
- AI가 연결 실패 사실을 고객에게 **안내하지 않음** (TTS 없음).

**문제:**
1. 담당자 연결처(SIP URI / 내선번호)가 설정되지 않은 상태에서 AI가 "연결해 드릴까요?"를 제안한 것 자체가 오류.
2. 연결 실패 후 고객에게 실패 안내 없이 침묵 → 고객이 재차 다른 질문으로 전환.

---

### 🔴 P3 — 통화 종료 후 LLM 처리 지속

**이벤트:** 통화 종료 시각 20:10:36, LLM 응답 완료 시각 20:11:03 (27초 후)

**상황:**
- "네 감사합니다." 발화(20:10:25)에 대해 LLM이 처리를 시작.
- 11초 후인 20:10:36에 통화가 종료됨.
- 그럼에도 LLM 처리가 계속돼 20:11:03에 응답 완료 및 TTS 푸시 시도.
- 종료된 통화에 HITL까지 발동 (`hitl_requested`).

**문제:** 통화 종료 이벤트가 LangGraph 파이프라인으로 전파되지 않아 불필요한 LLM 호출, HITL 오발동, TTS 시도가 발생. 리소스 낭비 + 운영자 혼선 유발.

---

### 🟡 P4 — 전 발화 HITL 과다 발동 (confidence 임계값 문제)

| 발화 | 신뢰도 | HITL 발동 | 판정 |
|---|---|---|---|
| 내일 날씨 | 0.27 | ✅ 발동 | 적절 (낮은 신뢰도) |
| 찾아가는 길 | 0.29 | ✅ 발동 | ⚠️ 응답은 적절했으나 발동 |
| 일반 기상 상식 | 0.34 | ✅ 발동 | 적절 (폴백 응답) |
| 감사합니다 (farewell) | 0.00 | ✅ 발동 | ❌ farewell에 HITL 불필요 |

**문제:**
- "찾아가는 길" 응답은 KB에서 정확한 답("택시타고 기상청에 가 주세요")을 찾았고 LLM이 적절히 가공해 응답했음에도 HITL이 발동됨. 신뢰도 0.29가 임계값 이하라는 이유만으로 HITL 처리.
- `farewell` 인텐트는 구조적으로 KB 매칭이 불가능(`category: farewell` 문서 없음)해 confidence=0.0이 나오지만 실제 응답은 정상적임. farewell에는 HITL 조건 예외 처리가 없음.

---

### 🟡 P5 — RAG 검색 품질 저하 (soft_fallback 전면 적용)

**현황:**
- 3건의 질문 모두 `strict_similarity_threshold(0.7)` 통과 문서 0건 → `soft_fallback(0.35)` 적용.
- 최고 유사도 스코어가 0.27~0.34 수준으로 매우 낮음.

**원인:**
- "내일 날씨" 질문에 대해 `cap_1004_forecast` (기능 설명 문서)만 매칭되고 실제 날씨 데이터 문서가 없음. 기상청 도메인임에도 날씨 데이터가 KB에 부재.
- "일반 기상 상식" 질문에 대해 `weather_warning` 카테고리 문서가 매칭됨 → 카테고리 불일치.
- "찾아가는 길" 질문에는 정확한 FAQ 문서(`kb_454bbfffb01d4f4d`)가 있었지만 score 0.22로 soft_fallback을 통해서만 검색됨 → 임베딩 품질 또는 FAQ 문서 작성 방식 개선 필요.

---

### 🟢 P6 — KB 답변 품질 문제 (찾아가는 길)

**KB 등록 내용:**
> "Q: 찾아오는 길을 알려주세요. A: 택시타고 기상청에 가 주세요. 라고 하면 됩니다."

**문제:** 실제 안내로 사용하기에 부적절한 답변("택시타고 기상청에 가 주세요. 라고 하면 됩니다"는 택시 기사에게 할 말을 안내하는 표현이지 고객에게 직접 하는 안내가 아님). LLM이 어느 정도 자연스럽게 가공했으나 KB 원문 자체를 수정해야 함.

---

## 3. 문제 요약표

| # | 문제 | 심각도 | 분류 |
|---|---|---|---|
| P1 | LLM 응답 지연 20~38초 (3회) | 🔴 높음 | 성능 |
| P2 | 담당자 연결 실패 + 실패 안내 없음 | 🔴 높음 | 기능 결함 |
| P3 | 통화 종료 후 LLM/HITL/TTS 계속 실행 | 🔴 높음 | 파이프라인 버그 |
| P4 | farewell 인텐트에 HITL 과다 발동 | 🟡 중간 | 로직 개선 |
| P5 | RAG soft_fallback 전면 적용 (KB 부실) | 🟡 중간 | 데이터 품질 |
| P6 | KB "찾아오는 길" 답변 부적절 | 🟢 낮음 | 콘텐츠 |

---

## 4. 권장 조치

### 즉시 (P1, P2, P3)
1. **LLM 타임아웃 설정**: `agent_elapsed > 10s`를 한계로 설정, 초과 시 대기 TTS("잠시 후 안내드리겠습니다") 선발송 후 계속 처리.
2. **담당자 연결 사전 검증**: 연결처(SIP URI) 미설정 시 AI가 "담당자 연결" 제안 자체를 하지 않도록 `get_transfer_target` 사전 조회 후 분기.
3. **통화 종료 이벤트 전파**: `call_ended` 이벤트 수신 시 진행 중인 LangGraph 태스크를 즉시 취소하고 HITL 발동 차단.

### 단기 (P4, P5)
4. **farewell HITL 예외 처리**: `intent == "farewell"`이면 confidence 무관하게 HITL 미발동.
5. **KB 보강**: 기상청 날씨 데이터 API 연동 or 주요 FAQ 실질 콘텐츠 추가. "찾아오는 길" FAQ 원문 수정.
6. **RAG 임계값 검토**: 현재 soft_floor 0.35도 너무 낮아 관련 없는 문서를 포함하는 경우가 있음. 카테고리별 임계값 차등 적용 고려.
